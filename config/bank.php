<?php

/**
 * 콜로 모 계좌 (요청서 5쪽, 2026-08-31).
 *
 * 계좌는 하나다(2026-08-31 회신). 환자 본인부담금도 기관 환급도 이 계좌로 들어온다 —
 * 무엇으로 들어온 돈인지는 금액과 보낸 사람으로 가린다.
 *
 * 팝빌 계좌조회는 이 계좌가 팝빌 사이트에 등록되고 정액제가 신청돼 있어야 돈다.
 */
return [
    'account' => [
        // 은행 코드 — 팝빌이 쓰는 세 자리(우리은행 020 · 국민 004 · 신한 088 …)
        'bank_code' => env('BANK_ACCOUNT_BANK_CODE', ''),
        'number'    => env('BANK_ACCOUNT_NUMBER', ''),
        'holder'    => env('BANK_ACCOUNT_HOLDER', ''),
    ],

    /*
     * 한 번에 얼마나 거슬러 올라가 긁을지.
     *
     * 은행이 늦게 반영하는 건이 있어 어제 것만 보면 놓친다. 겹쳐 읽어도 tid 로 걸러지니
     * 넉넉히 잡는다.
     */
    'sync_days' => (int) env('BANK_SYNC_DAYS', 7),
];
