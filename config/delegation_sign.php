<?php

/**
 * 운영 데이터 › 위임장 서명 (2026-09-14).
 *
 * 컨트롤러가 진작부터 delegation_sign.link_minutes 를 물었는데 이 파일이 없어
 * 언제나 기본값 30분으로 돌았다. 바꿀 수 있는 것처럼 보이면서 바뀌지 않았다.
 */
return [

    /*
     * 문자로 보낸 서명 링크가 열려 있는 동안 (분).
     *
     * 짧게 두는 것은 링크가 남의 손에 들어가도 곧 닫히게 하려는 것이다. 다만
     * 환자가 문자를 늦게 보면 다시 보내 달라는 연락이 온다 — 운영에서 그 연락이
     * 잦으면 늘린다.
     */
    'link_minutes' => (int) env('DELEGATION_SIGN_LINK_MINUTES', 30),

];
