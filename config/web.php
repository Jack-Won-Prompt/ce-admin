<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 웹 화면이 지금 시험 중인가, 운영 중인가
    |--------------------------------------------------------------------------
    |
    | 시험할 때는 화면에 적는 전화번호가 곧 문자가 가는 곳이다. 손으로 치다
    | 한 자만 틀려도 남의 전화로 안내가 가고, 맞게 쳐도 시험 문자가 실제
    | 환자에게 간다. 그래서 시험 중에는 CE Admin 에 등록된 사람의 번호
    | 가운데서 고르게 한다 — 문자가 우리 손 안에서만 돈다.
    |
    | 운영에서는 그대로 손으로 적는다. 환자 번호는 우리 사용자 목록에 있을
    | 까닭이 없다.
    |
    | 기본값은 'live' 다. 시험하려면 설정 › 서비스 연동 설정 › 웹 화면 설정
    | 에서 바꾼다 — .env 를 고치러 서버에 들어가지 않아도 되게 한 것이다.
    |
    */

    'mode' => env('WEB_MODE', 'live'),

    /*
    |--------------------------------------------------------------------------
    | 위드웍스 연계 확인 이메일
    |--------------------------------------------------------------------------
    |
    | 위드웍스로 주문ㆍ반품을 보낼 때 그 내용을 이 주소로도 보낸다.
    |
    | 연계가 잘 갔는지는 지금 저쪽 화면에 들어가 눈으로 찾아야 안다. 무엇을
    | 어떤 값으로 보냈는지는 로그에만 남고, 로그는 서버에 들어가야 읽는다.
    | 그래서 「보냈다는데 저쪽에 없다」가 생기면 무엇이 틀렸는지 짚는 데
    | 한참 걸렸다.
    |
    | 보낸 것을 그대로 받아 두면 저쪽 화면과 나란히 놓고 견줄 수 있다.
    |
    | 비우면 보내지 않는다. 테스트 설정에서 고친다.
    |
    */
    'withworks_email' => env('WEB_WITHWORKS_EMAIL', 'jhw7519@linkthelab.co.kr'),

];
