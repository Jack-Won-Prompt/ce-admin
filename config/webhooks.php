<?php

/*
|--------------------------------------------------------------------------
| 웹훅 관리 (2026-09-10 지시)
|--------------------------------------------------------------------------
|
| 밖과 주고받는 알림을 한자리에서 본다 — 무엇을 어디로 받고 보내는지(정의)와
| 실제로 무엇이 오갔는지(로그).
|
| 구분은 여기서 늘린다. 팝빌ㆍNICE 도 뒤에 붙는다(2026-09-10 예고).
|
*/

return [

    /** 구분 — 어느 바깥과 주고받는가 */
    'providers' => [
        'toss'      => '토스페이먼츠',
        'popbill'   => '팝빌',
        'nice'      => 'NICE 본인확인',
        'withworks' => '위드웍스',
        'ce_shop'   => 'CE 샵',
        'kakao'     => '카카오 알림톡',
        'etc'       => '기타',
    ],

    /** 방향 — 받는가 보내는가 */
    'directions' => [
        'inbound'  => '받음 (Inbound)',
        'outbound' => '보냄 (Outbound)',
    ],

    /** 파라미터가 어디에 실리는가 — 화면의 「위치」 칸이 읽는다 */
    'positions' => [
        'body'   => '본문',
        'header' => '헤더',
        'query'  => '주소 뒤',
        'path'   => '주소 안',
    ],

    /** 파라미터의 구분 — 화면의 「구분」 칸이 읽는다 */
    'types' => [
        'string'   => '문자',
        'int'      => '정수',
        'decimal'  => '소수',
        'bool'     => '예/아니오',
        'datetime' => '일시',
        'object'   => '객체',
        'array'    => '목록',
    ],

    /*
     * 로그에서 가릴 이름.
     *
     * 오간 값을 그대로 담되, 열쇠가 될 만한 것은 가려서 담는다 — 로그는 담당자가
     * 보는 자리고, 여기서 새면 서명 위조나 결제 조작으로 이어진다.
     */
    'mask_keys' => [
        'secret', 'signature', 'authorization', 'password', 'token',
        'secretkey', 'apikey', 'api_key', 'access_token', 'refresh_token',
        'cardnumber', 'card_number', 'residentno', 'resident_no', 'rrn',
    ],

    /*
     * 로그를 얼마나 두는가 (날).
     *
     * 쌓이는 자리라 언젠가 정리해야 한다. 지금은 값만 두고 지우지는 않는다 —
     * 지우는 일은 되돌릴 수 없어, 무엇을 얼마나 두어야 하는지 정한 뒤에 붙인다.
     */
    'log_days' => (int) env('WEBHOOK_LOG_DAYS', 180),
];
