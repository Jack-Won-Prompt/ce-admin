<?php

/**
 * 서버에서 난 잘못을 담는 규칙 (2026-09-11 지시).
 *
 * 화면에서 보는 자리는 「설정 › 오류 기록」이다.
 */
return [

    /* 4xx 는 대체로 부르는 쪽 사정이라 담지 않는다. 다만 아래 것은 자주 나면
       우리 얼개 문제라 남긴다 — 419 는 세션이 자꾸 끊기는 것이고,
       429 는 누가 너무 자주 두드리는 것이다. */
    'keep_status' => [419, 429],

    /* 같은 자리에서 같은 잘못이 며칠 안에 또 나면 줄을 새로 세우지 않고 셈만 올린다 */
    'merge_days' => 7,

    /* 목록에서 한 번에 보여 줄 최대 줄 수 */
    'list_limit' => 1000,

    /* 이보다 오래된 것은 「오래된 것 비우기」로 지운다 */
    'keep_days' => 180,
];
