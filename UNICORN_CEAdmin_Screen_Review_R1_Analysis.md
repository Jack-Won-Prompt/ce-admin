# UNICORN CE Admin — 화면 확정요청 1차 회신 분석 및 변경 반영 사양서

| 항목 | 내용 |
|---|---|
| 문서번호 | LTL-UNICORN-CRA-2026-0813-01 |
| 작성일 | 2026-08-13 |
| 작성 | 주식회사 링크더랩 |
| 원본 회신 자료 | `화면_확정요청_1차_-2026-08-13.pptx` (45장, Coloplast Korea 회신) |
| 대상 | UNICORN Healthcare Platform / Phase 1 CE Admin |
| 계약 | LTL-2026-UNICORN-001 |
| 상태 | **분석 완료 · 개발 반영 전 · 콜로플라스트 재확인 필요** |

---

## 0. 이 문서의 사용 방법

본 문서는 **3단계 워크플로우**의 중심 산출물입니다.

| 단계 | 수행 주체 | 입력 | 작업 | 출력 |
|---|---|---|---|---|
| **STEP 1** | 링크더랩 (분석) | 콜로 회신 PPTX | 회신 내용 항목화 · ID 부여 · 영향도 분석 | **본 MD 파일** |
| **STEP 2** | VS Code (Claude Code) | 본 MD + 소스코드 | §8 작업 지시에 따라 코드/DB/화면 실물 대조 → §7 `실현 가능성`·`작업량`·`확인 결과` 컬럼 채움 | 본 MD 갱신본 |
| **STEP 3** | Claude 웹 (Unicorn Project) | 본 MD 갱신본 | §10 지침에 따라 확인요청 PPT 생성 | 콜로 회신용 PPTX |
| **STEP 4** | 콜로플라스트 | 확인요청 PPT | §6 Q항목 회신 · 변경 승인 | 확정 → 개발 착수 |

> **중요** — STEP 2에서 VS Code가 코드를 확인하기 전까지, 본 문서의 `영향도`·`작업량` 값은 **회신 텍스트만 근거로 한 추정치**입니다. 실물 대조 후 반드시 갱신해야 합니다.

### 변경 유형 코드

| 코드 | 의미 | 일반적 영향 범위 |
|---|---|---|
| `RN` | 명칭 변경 (라벨/용어) | UI 라벨, 다국어 리소스 |
| `AD` | 항목(필드) 추가 | UI + DB 컬럼 + API 응답 |
| `DL` | 항목 삭제 | UI (DB는 보존 권장) |
| `MV` | 항목 위치 이동 (탭/섹션 간) | UI 배치, API 응답 그룹핑 |
| `LY` | 레이아웃 변경 (열 수, 정렬 순서) | UI 전용 |
| `FN` | 기능 신규/변경 | UI + API + 비즈니스 로직 |
| `LK` | 화면 간 또는 외부 시스템 연동 | API + 데이터 모델 |
| `NEW` | **신규 화면** (URS 범위 외 가능성) | 전 계층 + **CR 필요** |
| `Q` | 콜로플라스트 확인 필요 | — |

### 영향 계층 표기

`UI` 화면 · `DB` 스키마 · `API` 인터페이스 · `WW` WITHWORKS 연동 · `EXT` 기타 외부(Textract/Popbill/PG)

---

## 1. 회신 총괄 요약

### 1-1. 정량 요약

| 구분 | 건수 | 비고 |
|---|---|---|
| 전역 공통 변경 | 6 | 전 화면 일괄 적용 |
| 화면별 개별 변경 | 약 165 | 18개 화면 |
| 신규 화면 요청 | **7** | 반품/교환/취소 1 + Finance 6 |
| 콜로플라스트 확인 필요(Q) | 13 | §6 |
| 링크더랩 답변 필요(A) | 3 | §6-2 |
| **합계** | **약 190건** | |

### 1-2. 유형별 분포 (추정)

| 유형 | 건수 | 성격 |
|---|---|---|
| `RN` 명칭 변경 | 약 45 | 대부분 UI 라벨 — 작업량 小, 다만 **용어 사전 통일 선행 필요** |
| `AD` 항목 추가 | 약 70 | **DB 스키마 영향 다수** — 작업량 大 |
| `DL` 항목 삭제 | 약 8 | 작업량 小 |
| `MV` 항목 이동 | 약 12 | 화면 재구성 필요 |
| `LY` 레이아웃 | 약 15 | 3열 전환 · 정렬 순서 지정 |
| `FN`/`LK` 기능·연동 | 약 25 | **설계 재검토 필요 다수** |
| `NEW` 신규 화면 | 7 | **CR 대상** |

### 1-3. 핵심 판단 — 이번 회신의 성격

이번 회신은 **단순 화면 수정 요청이 아닙니다.** 다음 3가지 구조적 변경을 포함합니다.

| # | 구조적 변경 | 내용 | 파급 |
|---|---|---|---|
| **S-1** | **환자 마스터 개념 도입** | `거래처관리` → 유형(개인/병원/기관)으로 구분되는 **Master 화면**으로 재정의. 주문 화면의 환자 정보는 Master를 참조만 하고 **편집 불가(Block)** | 데이터 모델 재설계 · 주문↔환자 참조 방식 변경 |
| **S-2** | **반품/교환/취소 프로세스 신설** | 판매/교환/환불 3개 상태 체계, 3PL 연동 단계, 사유별 배송비 부담 자동 안내 | **신규 모듈** · WITHWORKS 역물류 연동 |
| **S-3** | **Finance 6개 내역 화면 신설** | 통합주문내역 · 정산내역(환자 10%) · 공단·지자체(90%) · 미정산 · 반품환불 · 부가세신고 | **신규 모듈** · PG/국세청 연동 확장 |

> **S-1 ~ S-3은 기존 확정 메뉴 28개 및 FRS M01~M05 범위를 초과할 가능성이 높습니다.** §9 CR 판정 참조.

---

## 2. 전역 공통 변경사항 (GL)

전 화면에 일괄 적용되어야 하는 항목입니다. **개별 화면 작업보다 먼저 처리**해야 중복 작업이 발생하지 않습니다.

| ID | 유형 | 요청 내용 | 적용 범위 | 영향 | 비고 |
|---|---|---|---|---|---|
| `GL-01` | `LY` | **모든 상세 정보 영역을 3열(3-column) 레이아웃으로 변경** | 거래처관리, 상담관리, 주문(환자정보/병원처방정보/추가정보) 등 전 상세 화면 | UI | 공통 폼 컴포넌트 단위로 처리 |
| `GL-02` | `FN` | **목록 표시 건수 제한 해제** — "전체 라인 다 보이게 제한 없이, 기본 설정" | 거래처관리, 처방전 목록, 주문 관리, 서류 관리 등 전 목록 화면 | UI + API | **⚠ 성능 리스크 — §2-1 별도 검토** |
| `GL-03` | `RN` | **용어 통일** — §2-2 용어 사전 일괄 적용 | 전 화면 + 문서 + DB 주석 | UI + DB | 사전 확정 후 일괄 치환 |
| `GL-04` | `AD` | **감사 항목 표준화** — `등록자` / `등록일` / `수정자` / `수정일자` 를 모든 상세 화면에 노출 | 전 상세 화면 | UI + DB + API | 기존 감사 컬럼 존재 여부 확인 필요 |
| `GL-05` | `FN` | **IC/OC 구분 표기** — IC 선택 시 환자명 앞에 `(E)` 표시. 예) `(E)김콜로` / OC 선택 시 `김콜로` | 환자명이 노출되는 전 화면 | UI | 표시 규칙 함수 공통화 |
| `GL-06` | `AD` | **엑셀 다운로드** — "모든 메뉴는 엑셀 다운로드 가능" (Finance 요청서 공통확인사항) | 전 목록 화면 | UI + API | 공통 익스포트 모듈로 구현 |

### 2-1. `GL-02` 표시 건수 무제한 — 링크더랩 검토 의견

| 항목 | 내용 |
|---|---|
| **요청 원문** | "표시 건수 : 전체 라인 다 보이게 제한 없이, 기본 설정이면 좋겠음" (2p, 3p, 9p, 26p, 32p 반복) |
| **문제점** | 환자·주문 데이터 누적 시 수만 건 단위가 되며, 전건 로딩은 브라우저 응답 지연·타임아웃·메모리 초과를 유발합니다. 특히 주문 관리 조회 결과는 컬럼 수가 60개 이상으로 요청되어 있어 부하가 가중됩니다. |
| **대안 A (권장)** | **가상 스크롤(Virtual Scroll) + 무한 스크롤** — 사용자 체감상 "전체가 보이는" 동작이면서 실제 렌더링은 화면 영역만 수행. 스크롤 시 자동 추가 로딩 |
| **대안 B** | 기본 표시 건수를 대폭 상향(예: 500건) + 하단 "전체 조회" 버튼 제공 |
| **대안 C** | 페이지 크기 선택 옵션(50/100/500/전체)을 사용자 설정으로 저장 |
| **결정 필요** | 콜로플라스트 확인 → `Q-01` |

### 2-2. 용어 사전 (`GL-03`) — 일괄 적용 대상

| # | AS-IS (현재 화면) | TO-BE (변경 후) | 근거 | 적용 화면 |
|---|---|---|---|---|
| T-01 | 보호자 이름 | **법정대리인 또는 가족 성명** | 위임장 양식 | 거래처관리, 주문, 위임장 서명, 주문관리 |
| T-02 | 보호자 생년월일 | **법정대리인 또는 가족 생년월일** | 위임장 양식 | 동상 |
| T-03 | 보호자 관계 | **가입자ㆍ피부양자와의 관계** | 위임장 양식 | 동상 |
| T-04 | 보호자 신분증 | **법정대리인 또는 가족 신분증** | 위임장 양식 | 거래처관리 |
| T-05 | 보호자명 | **송금자명** | 위드웍스 보호자명 정보 연결 | 주문 환자정보 |
| T-06 | 환자 부담 / 환자 부담금 / 환자 본인부담 | **본인부담금** | — | 주문정보, 주문연계, 주문관리 |
| T-07 | 급여 금액 / 급여 청구 금액 / 건보 청구액 / 총 NHIS 급여 | **기관 부담금** *(추후 재변경 가능)* | — | 주문정보, 주문연계, 주문관리 상세 |
| T-08 | 보험가 | **단가** | — | 주문정보 |
| T-09 | 처방전 여부 | **유형** (처방전-원내 / 처방전-원외 / 처방외) | — | 주문 상담정보 → 병원 처방정보로 이동 |
| T-10 | 보험 유형 | **자격** (+ `기타` 선택지 추가) | — | 병원 처방정보 |
| T-11 | Five/Six program | **Five/Six** (Five · Six · N/A 선택) | — | 병원 처방정보 |
| T-12 | Five(110days) | **Five/Six(110days)** | — | 병원 처방정보 (환자정보에서 이동) |
| T-13 | 판매거래처 유형 | **판매거래처** | — | 병원 처방정보 |
| T-14 | 처방전 관리 | **주문** | — | 메뉴 + 화면 제목 |
| T-15 | 처방전 확인 및 주문 | **주문** | — | 화면 제목 |
| T-16 | 처방전 검수 | **상세 목록** | — | 주문 화면 탭 |
| T-17 | 처방 제품 | **주문 정보** | — | 주문 화면 탭 |
| T-18 | 담당 | **검수 담당자** | — | 처방전 목록 |
| T-19 | 등록일 | **신환 Master 등록일** | — | 거래처관리 |
| T-20 | 재구매 예정일 | **재구매 가능일** | — | 재구매 관리 |
| T-21 | 기간 | **등록일자** | 위드웍스 거래처추가정보 기준 | 주문관리 검색조건 |
| T-22 | 카테터 | **IC** | — | 개인정보 동의 |
| T-23 | 장루 | **OC** | — | 개인정보 동의 |
| T-24 | 메모 | **검수 메모** | — | 주문 (상담정보 → 병원 처방정보로 이동) |
| T-25 | 거래처관리 | **Master (유형: 개인/병원/기관)** | 대표님 확인 필요 | 메뉴 · 전 참조 화면 → `Q-02` |

> **T-25는 미확정** — 메뉴 명칭 변경은 전 화면 네비게이션·권한·URL에 영향을 주므로, 확정 전까지 코드 반영을 보류합니다.

---

## 3. 화면별 변경 요청 상세

### 3-1. 거래처관리 — 환자 정보 (회신 3~5p)

**화면 코드: `CUS`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-CUS-01` | `AD` | 검색조건 추가: **IC/OC** | UI+API | 中 | ⚠ IC/OC 필드가 `patients` 에 없다 — 필드 신설(§11-1 #2) 후 검색 가능 |
| `CR-CUS-02` | `AD` | 검색조건 추가: **생성일자** (범위 검색, 예 `2026-08-01 ~ 2026-08-10`) | UI+API | 小 | ✅ `patients.created_at` 존재 — 범위 검색만 추가 |
| `CR-CUS-03` | `AD` | 검색조건 추가: **생년** | UI+API | 小 | ✅ `patients.birth_date` 존재 (단 값이 빈 건 다수 → `CR-SYS-92`) |
| `CR-CUS-04` | `AD` | 검색조건 추가: **건보위임종료일** | UI+API | 中 | ⚠ `counseling_data.udf43` — JSON 이라 인덱스 불가, 컬럼 승격 선행 |
| `CR-CUS-05` | `AD` | 검색조건 추가: **상병타입** | UI+API+DB | 中 | ⚠ 상병 타입 필드 자체가 없음(`disease_code` 와 다른 개념) |
| `CR-CUS-06` | `AD` | 검색조건 추가: **개인정보 동의** | UI+API | 中 | ⚠ `privacy_consents` 가 환자와 FK 로 묶여 있지 않다 — 이름·전화 매칭 규칙 필요 |
| `CR-CUS-07` | `AD` | 검색조건 추가: **건강보험공단 위임장 동의** | UI+API | 小 | ✅ `prescription_consents.status = agreed` 조인으로 가능 |
| `CR-CUS-08` | `DL` | **건보** 항목 삭제 (해당 정보 불필요) | UI | 小 | ✅ `patients.is_nhis_eligible` 화면 노출만 제거 |
| `CR-CUS-09` | `DL` | **건강보험번호 / 건보 적용** 삭제 (해당 정보 불필요) | UI | 小 | ✅ `health_insurance_no`·`is_nhis_eligible` 노출 제거 (DB 는 보존) |
| `CR-CUS-10` | `RN` | `등록일` → **신환 Master 등록일** (T-19) | UI | 小 | ✅ 라벨 변경 |
| `CR-CUS-11` | `LY` | 상세 정보 **3열** 표시 (GL-01) | UI | 中 | ✅ 공통 폼 컴포넌트 없음 — 화면별 재배치 필요 |
| `CR-CUS-12` | `AD`+`LK` | **송금자명** 추가 — **WITHWORKS 보호자명 정보와 연결 필요** | UI+DB+WW | 中 | ⚠ 송금자명 컬럼 없음 + 위드웍스 사양 미수령(`D-07`) |
| `CR-CUS-13` | `RN` | T-01 ~ T-04 용어 변경 (법정대리인 또는 가족 ~) | UI | 小 | ✅ 라벨만 변경 (값은 `prescription_consents.guardian_*` 에 존재) |
| `CR-CUS-14` | `LY` | **전체 상세 버튼을 앞쪽(좌측)으로 이동** — 예: `박일성` 이력 아래 | UI | 小 | ✅ 그리드 컬럼 순서 변경 |
| `CR-CUS-15` | `AD` | **선호 방식** 선택지 확장: 전화 / 해피톡 / 업무폰2586문자 / 업무폰2586카톡 / 업무폰3018문자 / 업무폰3018카톡 | UI+DB | 中 | ⚠ 선호 방식 컬럼 없음 → DDL 1 + 선택지 6종 |
| `CR-CUS-16` | `AD` | 환자 상세에 **등록자 / 수정자 / 수정일자** 모두 포함 (GL-04) | UI+DB+API | 中 | ⚠ `patients` 에 `created_by`·`updated_by` 없음 → DDL 2 + 기존 행 backfill 불가(이력 없음) |
| `CR-CUS-17` | `AD` | **생년월일 3종 병행 표기** — 아래 표 참조 | UI+DB | 小 | ✅ `birth_date` 1개에서 3가지 포맷 파생 가능 — `Q-03` 이 후자면 小, 각각 입력이면 中 |

**`CR-CUS-17` 생년월일 표기 규격**

| 필드 | 표기 예시 | 성격 |
|---|---|---|
| 생년월일(1) | `1982. 11. 11.` | 점 구분 · 공백 포함 |
| 생년월일(2) | `1982-11-11` | 하이픈 구분 (ISO) |
| 생년 | `1982` | 연도만 |

> **설계 의견** — 3개를 각각 DB 컬럼으로 저장하면 정합성이 깨집니다. **원본 1개 컬럼(DATE)** 을 저장하고 **표시 시점에 3가지 포맷으로 파생**하는 방식을 권장합니다. 단, 콜로 측이 3개를 각각 **입력·수정 가능한 독립 값**으로 원하는지 확인 필요 → `Q-03`

**`CR-CUS-18` 거래처관리 상세 필드 표시 순서 (3열)**

회신 5p에 명시된 순서를 그대로 반영합니다.

| 행 | 1열 | 2열 | 3열 |
|---|---|---|---|
| 1 | Active/Inactive | IC/OC | 환자명 |
| 2 | 구분(SB/SCI) | 상병 타입 | 주민등록번호 |
| 3 | 생년월일(1) | 생년월일(2) | 생년 |
| 4 | 성별 | Email | Fax |
| 5 | 주소 | 전화번호1 | 전화번호2 |
| 6 | 송금자명 | 현금영수증 | 현금영수증번호 |
| 7 | 서명여부 | 미성년 | 법정대리인 또는 가족 성명 |
| 8 | 법정대리인 또는 가족 생년월일 | 가입자ㆍ피부양자와의 관계 | 법정대리인 또는 가족 신분증 |
| 9 | 참고 사항 | | |
| 10 | 건보등록 | 건보등록일 | 건보재등록 대상자 |
| 11 | 건보 재등록 기한 | 건보위임동의시작일 | 건보위임동의종료일 |
| 12 | 기초(의료급여)재평가대상자 | 기초(의료급여)재평가기한 | |
| 13 | 등록자 | 수정자 | 수정일자 |
| 14 | 신환 master 등록일 | | |

> **신규 필드 판정 필요** — 위 34개 필드 중 현재 DB에 존재하지 않는 항목을 VS Code가 대조하여 목록화해야 합니다. §8-2 참조.

---

### 3-2. 상담 관리 (회신 6~7p)

**화면 코드: `CNS`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-CNS-01` | `LK` | **거래처관리 연결 필요** — 환자 정보를 거래처관리(Master)에서 참조 (5p 필드 구성 동일 적용) | UI+API | 大 | ⚠ 상담 자체가 `prescriptions.counseling_data` 라 환자 참조 구조가 없다 (§12) |
| `CR-CNS-02` | `LY` | 상세 **3열** 표시 | UI | 中 | ✅ 화면 재배치 |
| `CR-CNS-03` | `AD`+`LK` | **일일도뇨횟수 / Five/Six(110days) / 재구매 가능일 / Five/Six** 를 주문 화면에서 연결 표시 | UI+API | 中 | ✅ 값은 `counseling_data.daily_use_qty`·`five_program`·`five`·`udf30` 에 존재 |
| `CR-CNS-04` | `FN` | **화면 간 바로가기 버튼** — 상담 화면에서 주문 화면 등으로 즉시 이동하며 해당 환자 정보가 이어져 표시 | UI+FN | 中 | ✅ 라우팅 + 환자 컨텍스트 전달 |
| `CR-CNS-05` | `FN` | **모든 상담 내역 기록 보존** — 상담 내역 전건 이력 관리 | UI+DB+API | 中 | ⚠ 상담이 처방전당 JSON 1벌이라 이력이 남지 않는다 — 이력 표 신설 필요 |
| `CR-CNS-06` | `LY` | 상담관리 상세 필드 순서 — 거래처관리(3-1 표)와 **동일 구성 + `재구매 가능일` · `일일도뇨횟수` · `Five/Six` · `Five/Six(110days)` 추가** | UI | 中 | ⚠ `CR-CUS-18` 확정에 종속 |

---

### 3-3. 처방전 업로드 (회신 8p)

**화면 코드: `RXU`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-RXU-01` | `FN` | **업로드 최대 매수 10개 → 40개로 변경** | UI+API+EXT | 大 | ⚠ `prescription_images` 검증이 `max:10` (PrescriptionController:432). 상수는 小이나 Textract 4배·동기 처리 타임아웃으로 비동기 전환 수반 |

> **⚠ 영향도 경고** — 이 항목은 단순 상수 변경이 아닙니다.
> - **Amazon Textract 처리량 4배 증가** → OCR 처리 시간·비용 재산정 필요
> - 동기 처리 시 타임아웃 위험 → **비동기 큐 처리 전환 검토 필요**
> - S3 업로드 용량·타임아웃·진행률 UI 재설계
> - FR-009 (처방전 추출) 사양 갱신 필요
> - 회신 2p에도 동일 요청 반복("처방 자료 업로드 시, 40장까지 가능 하게 설정 필요")

---

### 3-4. 처방전 목록 (회신 9p)

**화면 코드: `RXL`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-RXL-01` | `AD` | **검수 일자** 추가 | UI+DB+API | 小 | ✅ `prescriptions.reviewed_at` 존재 |
| `CR-RXL-02` | `AD` | **처방자료 업로드 담당자** 추가 | UI+DB+API | 小 | ✅ `prescriptions.created_by` 존재 |
| `CR-RXL-03` | `AD` | **주민등록번호** 추가 | UI+API | 小 | ✅ `resident_no_ocr_masked` 존재 — 마스킹 표시(P0-1) |
| `CR-RXL-04` | `AD` | **병원명** 추가 | UI+API | 小 | ✅ `prescriptions.hospital_name` 존재 |
| `CR-RXL-05` | `FN` | **라인별 상태(status) 표시** — 우선 `검수 필요` / `대기중` / `검수 완료` 3종으로 단순화 | UI+DB | 中 | ⚠ 현재 상태 7종(`pending`~`ordered`) → 3종 매핑 규칙 확정 필요 |
| `CR-RXL-06` | `Q` | **`OCR 처리중`이 `대기중`과 동일한 의미인지 확인 필요** | — | — | → `Q-04` |
| `CR-RXL-07` | `RN` | `담당` → **검수 담당자** (T-18) | UI | 小 | ✅ 라벨 변경 |
| `CR-RXL-08` | `FN` | 표시 건수 제한 해제 (GL-02) | UI+API | — | ⚠ `Q-01` 선행 — 현재 서버 페이지네이션 + wwGrid 전량 렌더 (`CR-SYS-94`) |
| `CR-RXL-09` | `AD` | **검수 메모** 추가 — 예: `서류 미비` / `서류 수정` | UI+DB+API | 小 | ✅ `prescriptions.review_memo` 존재 |

> **`CR-RXL-09` 연계** — 검수 메모는 주문 화면 병원 처방정보에도 연결·Block 표시되어야 함 (`CR-ORH-11` 참조)

---

### 3-5. 메뉴 구조 재편 (회신 10p)

**화면 코드: `MNU`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-MNU-01` | `RN` | 메뉴 `처방전 관리` → **주문** (T-14) | UI+라우팅 | 中 | ⚠ 사이드바·`config/permissions.php` 페이지키·라우트명·권한 그룹을 함께 고쳐야 함 |
| `CR-MNU-02` | `LY` | **`주문`을 `주문 재구매` 하위그룹으로 이동** | UI+네비게이션 | 小 | ✅ 사이드바 그룹 재배치 |
| `CR-MNU-03` | `LY` | **`CE샘플판매주문`을 하위그룹으로 이동** | UI+네비게이션 | 小 | ✅ 동상 |
| `CR-MNU-04` | `LY`+`NEW` | **`교환/반품/취소`를 하위그룹에 신설·배치** | UI+네비게이션 | 小 | ✅ 배치는 小 — 화면 자체는 `CR-RTN-*` (CR 필요) |

**TO-BE 메뉴 구조 (회신 10p 명시)**

```
주문 재구매
└─ 하위그룹
   ├─ 주문                  ← 기존 "처방전 관리" 명칭 변경
   ├─ 교환/반품/취소          ← 신규 (§3-11)
   └─ CE 샘플판매주문         ← 기존, 위치 이동
주문 관리
현황
```

> **영향** — 확정된 CE Admin 28개 메뉴 구조(문서번호 LTL-UNICORN-CFM-2026-0812-01)의 그룹 배치가 변경됩니다. 확인서 재발행 필요 여부 판단 → `Q-05`

---

### 3-6. 주문 — 화면 전반 (회신 11p)

**화면 코드: `ORD`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-ORD-01` | `RN` | 화면명 `처방전 관리` → **주문** | UI | 小 | ✅ 화면 제목·breadcrumb |
| `CR-ORD-02` | `AD`+`FN` | **주문 목록 추가** — 누적된 모든 주문 list 표시 | UI+API | 大 | ⚠ 검수 화면은 처방전 1건 상세 구조 — 누적 주문 목록은 별도 화면 신설 |
| `CR-ORD-03` | `LY` | 위치: `환자 처방` → **`주문 재구매` 하위그룹**. 위드웍스처럼 **한 화면에서 유형별로 주문 목록 조회** | UI+API | 中 | ⚠ 유형 필드(`NEW`) 확정 후 가능 |
| `CR-ORD-04` | `RN` | `처방전 확인 및 주문` → **주문** (T-15) | UI | 小 | ✅ 라벨 변경 |
| `CR-ORD-05` | `AD` | **위임 동의 앞에 `개인정보 동의` 버튼 추가** | UI+API | 中 | ⚠ `privacy_consents` 가 처방전·환자와 FK 로 묶여 있지 않다 — 연결 규칙 필요 |
| `CR-ORD-06` | `RN` | 탭 `처방전 검수` → **상세 목록** (T-16) | UI | 小 | ✅ 탭 라벨 |
| `CR-ORD-07` | `RN` | 탭 `처방 제품` → **주문 정보** (T-17) | UI | 小 | ✅ 탭 라벨 |

---

### 3-7. 주문 — 상담 정보 섹션 (회신 12p)

**화면 코드: `ORC`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-ORC-01` | `FN` | **상담번호 자동 채번** — 수기 입력 불가(block) 처리 | UI+API+DB | 小 | ✅ `Prescription::generateCounselNo()` + `prescriptions.counselNo` 라우트 이미 존재 — 입력 차단만 |
| `CR-ORC-02` | `RN`+`MV` | `처방전 여부` → **유형** 으로 변경, 선택지 `처방전-원내` / `처방전-원외` / `처방외`, **위치를 병원 처방정보로 이동** (T-09) | UI+DB | 中 | ⚠ 유형 필드가 없음(`NEW`) + 섹션 이동 |
| `CR-ORC-03` | `DL` | **재상담 일자** 삭제 (해당 정보 불필요) | UI | 小 | ✅ `counseling_data.re_counsel_date` 노출 제거 |
| `CR-ORC-04` | `DL` | 상담 유형에서 **개인구매** 선택지 삭제 | UI+DB | 小 | ✅ 선택지 제거 |
| `CR-ORC-05` | `DL` | 상담 상태에서 **재상담** 선택지 삭제 | UI+DB | 小 | ✅ 선택지 제거 |
| `CR-ORC-06` | `RN`+`MV` | `메모` → **검수 메모** 로 변경, **병원 처방정보로 이동** (T-24) | UI+DB | 小 | ✅ 이동. 단 `counsel_memo`(JSON)와 `review_memo`(컬럼)가 공존 — 정본 지정 필요(`CR-SYS-91`) |

---

### 3-8. 주문 — 환자 정보 섹션 (회신 13~15p)

**화면 코드: `ORP`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-ORP-01` | `LK`+`FN` | **거래처관리(Master) 환자정보와 연결**, 주문 화면에서는 **편집 Block**. 정보 변경은 거래처관리에서만 수행 | UI+API+**데이터모델** | 大 | ❌ 현 구조 불가 — 주문은 환자 값을 스냅샷으로 복사하고 마스터→주문 전파가 없다 (§12) |
| `CR-ORP-02` | `AD` | **선호 방식** 추가 (6종, `CR-CUS-15`와 동일) | UI+DB | 中 | ⚠ 선호 방식 컬럼 없음 (`CR-CUS-15` 와 동일 DDL) |
| `CR-ORP-03` | `AD` | **Active/Inactive** 추가 | UI+DB | 小 | ⚠ Active/Inactive 컬럼 없음 → DDL 1 |
| `CR-ORP-04` | `RN` | `생년월일` → **생년월일(1)** | UI | 小 | ✅ 라벨 변경 |
| `CR-ORP-05` | `AD` | **생년월일(2)** 추가 | UI | 小 | ✅ `birth_date` 파생 |
| `CR-ORP-06` | `AD` | **생년** 추가 | UI | 小 | ✅ `birth_date` 파생 |
| `CR-ORP-07` | `RN`+`LK` | `보호자명` → **송금자명**, **위드웍스 보호자명 정보 연결** (T-05) | UI+WW | 中 | ⚠ 송금자명 컬럼 없음 + `D-07` 미수령 |
| `CR-ORP-08` | `AD` | **Fax** 추가 | UI+DB | 小 | ⚠ Fax 컬럼 없음 → DDL 1 |
| `CR-ORP-09` | `AD` | **상병 타입** 추가 | UI+DB | 小 | ⚠ 상병 타입 컬럼 없음 → DDL 1 |
| `CR-ORP-10` | `MV` | **구분(SB/SCI)** — 병원 처방정보 → **환자 정보로 이동** | UI | 小 | ✅ `counseling_data.udf6` 존재 — 표시 위치만 이동 |
| `CR-ORP-11` | `FN` | **현금영수증 발행 구분** — `소득공제` 고정 문구 삭제, `소득공제`/`지출증빙`/`자진발급` 선택 가능. **`자진발급` 선택 시 번호를 `010-000-1234` 로 자동 입력** | UI+DB+EXT | 中 | ⚠ `cash_receipt_type` 이 `income_deduction`/`business_expense` 2종 — `자진발급` 추가 + 번호 자동입력 |
| `CR-ORP-12` | `FN` | **건보 재등록 대상자** Y/N 선택 가능 | UI+DB | 小 | ✅ `counseling_data.udf4` 존재 — Y/N 선택지로 변경 |
| `CR-ORP-13` | `RN`+`MV` | `Five(110days)` → **Five/Six(110days)**, 환자 정보 → **병원 처방정보로 이동** (T-12) | UI | 小 | ✅ `counseling_data.five` 존재 — 이동 |
| `CR-ORP-14` | `RN`+`AD` | T-01~T-03 용어 변경 **및 항목 추가** (법정대리인 또는 가족 성명·생년월일·관계) | UI+DB | 小 | ✅ `prescription_consents.guardian_*` 존재 — 라벨 변경 + 검수 화면 노출 |
| `CR-ORP-15` | `AD`+`LK` | **신환 master 등록일** 추가 — 거래처관리 등록일과 연결, 환자 정보로 이동 | UI+API | 中 | ⚠ `counseling_data.udf32` 와 `patients.created_at` 두 값이 있어 의미 확정 필요 |
| `CR-ORP-16` | `MV` | **일일 도뇨 횟수** — 환자 정보 → **병원 처방 정보로 이동** | UI | 小 | ✅ `counseling_data.daily_use_qty` 존재 — 이동 |
| `CR-ORP-17` | `AD`+`LK` | **참고 사항** 추가 — 거래처관리 환자정보와 연결 | UI+API | 小 | ⚠ `patients.note`/`memo` 두 컬럼 공존 — 정본 지정 후 참조(`CR-SYS-91`) |
| `CR-ORP-18` | `LY` | 3열 표시 순서 — 아래 표 | UI | 中 | ⚠ `CR-CUS-18` 과 동일 필드 집합 — 추가 DDL 없음, 배치만 (§11-2) |

**`CR-ORP-18` 주문 화면 환자 정보 3열 순서 (회신 15p)**

| 행 | 1열 | 2열 | 3열 |
|---|---|---|---|
| 1 | Active/Inactive | IC/OC | 환자명 |
| 2 | 구분(SB/SCI) | 상병 타입 | 주민등록번호 |
| 3 | 생년월일(1) | 생년월일(2) | 생년 |
| 4 | 성별 | Email | Fax |
| 5 | 주소 | 전화번호1 | 전화번호2 |
| 6 | 송금자명 | 현금영수증 | 현금영수증번호 |
| 7 | 법정대리인 또는 가족 성명 | 법정대리인 또는 가족 생년월일 | 가입자ㆍ피부양자와의 관계 |
| 8 | 참고 사항 | | |
| 9 | 건보등록 | 건보등록일 | 건보재등록 대상자 |
| 10 | 건보 재등록 기한 | 건보위임동의시작일 | 건보위임동의종료일 |
| 11 | 기초(의료급여)재평가대상자 | 기초(의료급여)재평가기한 | |
| 12 | 신환 master 등록일 | | |

> **거래처관리(3-1)와의 차이** — 주문 화면에는 `서명여부`·`미성년`·`법정대리인 또는 가족 신분증`·`등록자/수정자/수정일자` 가 제외되어 있습니다. 의도적 차이인지 누락인지 확인 필요 → `Q-06`

---

### 3-9. 주문 — 병원 처방 정보 섹션 (회신 16~17p)

**화면 코드: `ORH`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-ORH-01` | `AD` | **판매거래처 추가** — 어느 업체에서 진행하는지 식별 (예: 콜로플라스트, 메디칼써프라이 …) | UI+DB+API | 中 | ⚠ 판매거래처 컬럼 없음. `master_items(category=dealer)` 를 참조로 쓰면 재사용 가능 |
| `CR-ORH-02` | `RN`+`MV` | `처방전 여부` → **유형** (원내/원외/처방외), 상담정보에서 이동 (T-09) | UI+DB | 中 | ⚠ 유형 필드 `NEW` + 이동 |
| `CR-ORH-03` | `RN`+`MV` | `Five(110days)` → **Five/Six(110days)**, 환자정보에서 이동 (T-12) | UI | 小 | ✅ `counseling_data.five` 존재 — 이동 |
| `CR-ORH-04` | `RN`+`AD` | `보험 유형` → **자격**, 선택지에 **`기타` 추가** (T-10) | UI+DB | 小 | ✅ `counseling_data.udf11` 존재 — 선택지에 `기타` 추가 |
| `CR-ORH-05` | `RN`+`FN` | `Five/Six program` → **Five/Six**, 선택지 `Five` / `Six` / `N/A` (T-11) | UI+DB | 小 | ✅ `counseling_data.five_program` 존재 — 선택지 3종 고정 |
| `CR-ORH-06` | `AD` | **등록담당자 / 등록일 / 수정자 / 수정일자 / System Operator** 추가 | UI+DB+API | 中 | ⚠ `created_by`·`updated_by`·`created_at`·`updated_at` 존재(화면 미노출). `System Operator` 만 `NEW` |
| `CR-ORH-07` | `RN` | `판매거래처 유형` → **판매거래처** (T-13) | UI | 小 | ✅ 라벨 변경 |
| `CR-ORH-08` | `AD` | **진료과목** 추가 — 선택지: 비뇨기과 / 재활의학과 / 신경과 / 신경외과 / 정형외과 | UI+DB | 小 | ✅ `prescriptions.specialty` 존재 — 선택지 5종 고정. `department` 와 중복(`CR-SYS-91`) |
| `CR-ORH-09` | `AD`+`LK` | **신환 master 등록일** — 거래처관리 등록일과 연결 | UI+API | 中 | ⚠ `CR-ORP-15` 와 동일 사안 |
| `CR-ORH-10` | `MV` | **일일 도뇨 횟수** — 환자정보에서 이동 | UI | 小 | ✅ 이동 |
| `CR-ORH-11` | `AD`+`LK` | **검수 메모** 추가 — **처방전 목록 화면의 검수 메모(`CR-RXL-09`)와 연결하고 Block(읽기전용) 처리** | UI+API | 小 | ✅ `prescriptions.review_memo` 존재 — 읽기전용 표시 |
| `CR-ORH-12` | `LY` | 3열 표시 순서 — 아래 표 | UI | 中 | ⚠ 32필드 중 `NEW` 7 · `RENAME` 17 (§11-3) |

**`CR-ORH-12` 병원 처방 정보 3열 순서 (회신 17p)**

| 행 | 1열 | 2열 | 3열 |
|---|---|---|---|
| 1 | 검수 메모 | 설명 | |
| 2 | 요양병원코드 | 병원명 | 판매거래처 |
| 3 | 유형 | 신구매/재구매 | |
| 4 | 진단 확인일 | 상병구분 | 상병코드 |
| 5 | 요류역학검사일 | 자격 | |
| 6 | 1일 처방개수 | 총 처방기간 | 총계 |
| 7 | 처방전발행일 | 처방전사용기간 (교부일로부터) | 처방전종료일 |
| 8 | 담당 의사명 | 진료과목 | 사유 |
| 9 | 일일 도뇨횟수 | Five/Six | Five/Six(110days) |
| 10 | 주문 담당자 | System Operator | 환급해당기관 |
| 11 | 결제일 | 구입일(모든서류 발행일) | 사용 시작일(사용개시일) |
| 12 | 급여종료일(사용종료일) | 다음 재구매 가능일 | |

---

### 3-10. 주문 — 추가 정보 섹션 (회신 18p)

**화면 코드: `ORE`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-ORE-01` | `AD` | **처방전 업로드 담당자** 추가 | UI+DB+API | 小 | ✅ `prescriptions.created_by` 존재 |
| `CR-ORE-02` | `AD` | **검수 담당자** 추가 | UI+DB+API | 小 | ✅ `prescriptions.reviewed_by` 존재 |
| `CR-ORE-03` | `AD` | **수정 담당자** 추가 | UI+DB+API | 小 | ✅ `prescriptions.updated_by` 존재 |
| `CR-ORE-04` | `AD` | **등록 담당자** 추가 | UI+DB+API | 小 | ✅ `prescriptions.created_by` 존재 |
| `CR-ORE-05` | `FN` | **담당자 자동 결정 규칙** — 아래 표 | 비즈니스로직 | 中 | ⚠ 규칙의 분기 기준인 `유형`(원내/원외/처방외)이 아직 없는 필드 |
| `CR-ORE-06` | `LY` | 3열 표시 | UI | 小 | ✅ 화면 재배치 |

**`CR-ORE-05` 유형별 담당자 자동 표시 규칙**

| 유형 | 처방전 업로드 담당자 | 등록자 |
|---|---|---|
| 처방전-원내 / 처방전-원외 | 실제 업로드 수행자 | **업로드 담당자와 동일하게 표시** |
| 처방외 | **공란** | `신규 등록` 버튼을 눌러 해당 오더라인을 생성한 담당자 |

---

### 3-11. 주문 정보 (구 처방 제품) (회신 19~22p)

**화면 코드: `ORI`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-ORI-01` | `RN` | `처방 제품` → **주문 정보** (T-17) | UI | 小 | ✅ 탭 라벨 |
| `CR-ORI-02` | `Q` | **판매 유형** 항목 필요 여부 확인 필요 | — | — | → `Q-07` |
| `CR-ORI-03` | `AD` | 처방 제품 정보 라인에 **유형 / 자격 / 비율** 추가 | UI+DB+API | 中 | ⚠ `prescription_items` 에 유형·자격·비율 3컬럼 추가 필요 |
| `CR-ORI-04` | `RN` | `총 NHIS 급여` → **총 기관 부담금** (T-07) | UI | 小 | ✅ 라벨 변경 |
| `CR-ORI-05` | `RN` | `보험가` → **단가** (T-08) | UI | 小 | ✅ 라벨 변경 |
| `CR-ORI-06` | `RN` | `급여 금액` → **기관 부담금** (T-07, 추후 재변경 가능) | UI | 小 | ✅ 라벨 변경 |
| `CR-ORI-07` | `RN` | `환자 부담` → **본인 부담금** (T-06) | UI | 小 | ✅ 라벨 변경 |
| `CR-ORI-08` | `AD` | **공급가액 / 부가세 / 금액** 표시 추가 | UI+API | 小 | ✅ 금액에서 파생 계산 (현재 현금영수증·세금계산서에서 이미 같은 계산 사용) |
| `CR-ORI-09` | `Q` | **주문 연계 탭과 통합 여부 확인 필요** | — | — | → `Q-08` |
| `CR-ORI-10` | `AD`+`WW` | **Lot Number** 추가 | UI+DB+WW | 中~大 | ❌ 위드웍스 WMS 로트 관리 여부 미확정 — `Q-09` 회신 전 착수 불가 |
| `CR-ORI-11` | `AD`+`WW` | **유효기간** 추가 | UI+DB+WW | 中~大 | ❌ 동상 |

> **`CR-ORI-10` / `CR-ORI-11` 중대 영향** — Lot / 유효기한 관리는 WITHWORKS 물류 측 **로트 추적 운영 여부**와 직결됩니다. 물류 연계 준비사항 문서(LTL 물류연계)의 미확정 항목 "로트·유효기한 추적 관리 적용 여부"와 동일 사안이며, **WITHWORKS WMS가 로트를 관리하지 않으면 UNICORN 단독 구현이 불가능**합니다. → `Q-09`

**`CR-ORI-12` 유형·자격별 부담 비율 매트릭스 (회신 20p) — 신규 비즈니스 규칙**

| 유형 | 자격 | 비율 (위드웍스 청구전략과 동일 의미) | 현금영수증 | 전자세금계산서(주민번호 발행) |
|---|---|---|---|---|
| 처방전-원내 / 원외 | 일반 | 본인부담금 10% + 건강보험공단 부담금 90% | 10% | 90% |
| 처방전-원내 / 원외 | 차상위경감 | 본인부담금 0% + 건강보험공단 부담금 100% | 0% | 100% |
| 처방전-원내 / 원외 | 기초 | 본인부담금 0% + 지자체(시군구청) 부담금 100% | 0% | 100% |
| 처방전-원내 / 원외 | 산재 | 본인부담금 100% *(부가세 발행 방식 확인중)* | 0% | 100% |
| 처방전-원내 / 원외 | 자동차보험 | *(추후 확인되면 공유 예정)* | 확인중 | 확인중 |
| 처방외 | 처방 아님 | 본인부담금 100% | 100% | 0% |

> **이 매트릭스는 금액 계산·세무 발행의 근간이 되는 규칙표입니다.** 산재·자동차보험 2개 행이 미확정이므로 해당 자격의 주문은 개발 시 **예외 처리 또는 선택 비활성화**로 두고, 확정 후 활성화하는 방식을 권장합니다. → `Q-10`, `Q-11`

---

### 3-12. 주문 연계 (회신 23p)

**화면 코드: `ORL`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-ORL-01` | `Q` | 주문 정보 탭과 **통합 여부 확인 필요** (`CR-ORI-09`와 동일 사안) | — | — | → `Q-08` |
| `CR-ORL-02` | `RN` | `급여 청구 금액` → **기관 부담금** (T-07) | UI | 小 | ✅ 라벨 변경 |
| `CR-ORL-03` | `RN` | `환자부담(급여 적용 후)` → **본인부담금** (T-06) | UI | 小 | ✅ 라벨 변경 |
| `CR-ORL-04` | `AD` | **자격** 포함 | UI+API | 小 | ✅ `counseling_data.udf11` 존재 |
| `CR-ORL-05` | `AD` | **비율** 포함 | UI+API | 小 | ⚠ 비율은 현재 `nhis_status` 3종에 90/50/0% 고정 — `CR-ORI-12` 매트릭스로 교체 후 표시 |

---

### 3-13. 【신규】 반품 / 교환 / 취소 (회신 24p)

**화면 코드: `RTN` · 유형 `NEW` · ⚠ CR 대상**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-RTN-01` | `NEW` | **원 주문 정보** 표시 | 전 계층 | 大 | ❌ 해당 모듈·표 없음 — CR 필요 |
| `CR-RTN-02` | `NEW` | **담당자** 표시 | 전 계층 | 中 | ❌ 동상 |
| `CR-RTN-03` | `NEW` | **각 단계별 사유** 관리 | 전 계층 | 中 | ❌ 동상 |
| `CR-RTN-04` | `NEW` | **상태** 관리 (3개 상태 체계, 아래) | 전 계층 | 大 | ❌ `orders.status` 는 현재 `pending`/`confirmed` 2종뿐 — 3개 상태 체계 신설 |
| `CR-RTN-05` | `NEW` | **제품 정보** 표시 | 전 계층 | 中 | ❌ 동상 |
| `CR-RTN-06` | `NEW`+`WW` | **3PL과 정보 확인·공유 단계** | 전 계층+WW | 大 | ❌ 위드웍스 역물류 API 미정의 — 물류연계 문서와 상태코드 통일 선행 |
| `CR-RTN-07` | `NEW`+`FN` | **사유별 배송비 부담 주체 자동 안내** — 단순변심 / 상품불량 / 오배송 / 사이즈 교환 | 비즈니스로직 | 中 | ❌ 동상 |
| `CR-RTN-08` | `NEW`+`FN` | **교환 vs 반품 선택**, 교환 시 옵션(사이즈/수량) 재선택 | UI+FN | 中 | ❌ 동상 |
| `CR-RTN-09` | `NEW`+`FN` | **환불 수단** 추가 | UI+DB+EXT(PG) | 中 | ❌ 토스 결제취소 API 연동 확장 필요 |
| `CR-RTN-10` | `NEW`+`FN` | **수거 방법** — 택배 자동수거 / 직접발송 + 환불계좌 or 재배송지 입력 | UI+WW | 中 | ❌ 동상 |
| `CR-RTN-11` | `NEW`+`FN` | **신청 완료 시 접수번호 발급 → 진행상황 확인 페이지 연결** | UI+API | 中 | ❌ 동상 |
| `CR-RTN-12` | `Q` | **승인 단계 위치 확정 필요** (교환·환불 각각) | — | — | → `Q-12` |

**`CR-RTN-04` 상태 체계 (회신 24p)**

| 구분 | 상태 흐름 |
|---|---|
| 판매상태 | 결제완료 → 출고 대기 → 출고 완료 → 배송중 → 배송완료 → 구매확정 |
| 교환상태 | 접수 → 수거중 → 검수중 → 재발송 → 완료 / 취소 *(승인 단계 위치 확인 필요)* |
| 환불상태 | 접수 → 수거중 → 검수중 → 확인요청 → 환불승인 → 환불완료 / 취소 *(승인 단계 위치 확인 필요)* |

> **물류 연계 문서와의 정합성** — 이 상태 체계는 앞서 작성한 「WITHWORKS 물류연계 준비사항」의 역방향 물류 프로세스(반품 신청 → 승인 → 회수 접수 → 회수 입고 → 검수 → 재고 처리 → 결과 회신 → 환불)와 **매핑되어야** 합니다. 두 문서의 상태 코드를 통일하는 작업이 필요합니다. → §9 참조

---

### 3-14. CE 샘플 판매주문 (회신 25p)

**화면 코드: `SMP`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-SMP-01` | `LK`+`WW` | **위드웍스 해당 화면 연결 필요** | API+WW | 中~大 | ❓ 위드웍스 화면·API 사양 미수령(`D-10`) |
| `CR-SMP-02` | `LY` | `주문 재구매` 하위그룹으로 이동 (`CR-MNU-03`) | UI | 小 | ✅ 사이드바 이동 |
| `CR-SMP-03` | `AD`+`FN` | **이미지 업로드 버튼 추가** (추후 관련 이미지 업로드 가능성) | UI+API+S3 | 中 | ✅ `prescription_attachments` 업로드 구조 재사용 가능 |

---

### 3-15. 주문 관리 — 검색 조건 (회신 26p)

**화면 코드: `ORM`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-ORM-01` | `RN`+`AD` | `기간` → **등록일자** 로 변경 및 추가 (위드웍스 거래처추가정보 방식) | UI+API | 小 | ✅ `orders.created_at` 존재 — 라벨 + 범위 검색 |
| `CR-ORM-02` | `FN` | 기준/정렬 — 표시 건수 제한 해제 (GL-02) | UI+API | — | ⚠ `Q-01` 선행 (`CR-SYS-94`) |
| `CR-ORM-03` | `Q`+`RN` | **`상태`가 위드웍스 `판매상태 2`와 동일 의미이면 명칭 변경** | UI | 小 | → `Q-13` |
| `CR-ORM-04` | `AD` | **처방전 종료일** 검색조건 추가 | UI+API | 中 | ⚠ `counseling_data.udf14` — JSON 검색 불가, 컬럼 승격 선행 |
| `CR-ORM-05` | `AD` | **병원명** 추가 | UI+API | 小 | ✅ `prescriptions.hospital_name` 조인 |
| `CR-ORM-06` | `AD` | **요양병원코드** 추가 | UI+API | 小 | ✅ `prescriptions.hospital_code` 조인 (`erp_cd9` 와 중복 — `CR-SYS-91`) |
| `CR-ORM-07` | `AD` | **판매거래처** 추가 | UI+API | 中 | ⚠ 판매거래처 필드 `NEW` (`CR-ORH-01` 선행) |
| `CR-ORM-08` | `AD` | **다음 재구매가능일** 추가 | UI+API | 中 | ⚠ `prescriptions.repurchase_date` 와 `counseling_data.udf30` 중복 — 정본 지정 선행 |
| `CR-ORM-09` | `AD` | **송금자명** 추가 | UI+API | 中 | ⚠ 송금자명 `NEW` |
| `CR-ORM-10` | `AD` | **유형** 추가 | UI+API | 中 | ⚠ 유형 `NEW` |
| `CR-ORM-11` | `AD` | **신구매/재구매** 추가 | UI+API | 中 | ⚠ `counseling_data.udf17` — 컬럼 승격 선행 |
| `CR-ORM-12` | `AD` | **결제 수단** 추가 | UI+API | 中 | ❓ 결제수단이 저장되는 곳 미확인 — 토스 결제 연동 표 확인 필요 |
| `CR-ORM-13` | `LK` | **반품/교환/취소 정보 연결** | API | 中 | ❌ 반품/교환/취소 모듈 자체가 없음 (`CR-RTN-*` 종속) |
| `CR-ORM-14` | `AD` | **출고 상태** 추가 | UI+API+WW | 小 | ✅ `orders.withworks_ship_status_label` 존재 |
| `CR-ORM-15` | `AD` | **결제일** 추가 | UI+API | 中 | ⚠ `counseling_data.pay_date` — 컬럼 승격 선행 |

---

### 3-16. 주문 관리 — 조회 결과 (회신 27p)

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-ORM-16` | `Q` | **주문유형** 항목 필요 여부 확인 필요 | — | — | → `Q-07` |
| `CR-ORM-17` | `RN` | `환자 부담금` → **본인 부담금** (T-06) | UI | 小 | ✅ 라벨 변경 |
| `CR-ORM-18` | `AD` | **기관 부담금** 추가 | UI+API | 小 | ✅ `orders.nhis_amount` 존재 |
| `CR-ORM-19` | `AD` | **제품 코드** 추가 | UI+API | 小 | ✅ `orders.product_code` 존재 |
| `CR-ORM-20` | `AD` | **제품 관리번호(장비코드)** 추가 — *supportworks에 관련 파일 있음, 엑셀 필요 시 Stella에게 연락* | UI+DB+API | 中 | ⚠ 컬럼 없음 + 엑셀(`D-05`) 미수령 |
| `CR-ORM-21` | `LY`+`AD` | **조회 결과 컬럼 순서 지정 및 미존재 항목 신규 추가** — 아래 목록 | UI+DB+API | 大 | ⚠ 58컬럼 중 `EXISTS` 22 · `RENAME` 24 · `NEW` 12 (§11-4). `Q-15`(컬럼 선택) 권고 |

**`CR-ORM-21` 주문 관리 조회 결과 컬럼 순서 (회신 27p 원문 순서)**

> 총 **62개 컬럼**. 현재 화면에 없는 항목은 **추가 필요**.

| # | 컬럼 | # | 컬럼 | # | 컬럼 |
|---|---|---|---|---|---|
| 1 | 주문번호 | 22 | 판매 거래처명 | 43 | 상병 구분 |
| 2 | 유형 | 23 | 주문담당자 | 44 | 1일 처방개수 |
| 3 | 신구매/재구매 | 24 | 처방전 종료일 | 45 | 총계 |
| 4 | 처방전 발행일 | 25 | 판매상태 | 46 | 마지막 확정수량 |
| 5 | 환자명 | 26 | 판매상태2 | 47 | 하루 사용수량 |
| 6 | 주민등록번호 | 27 | 출고상태 | 48 | 처방전사용기간(교부일로부터) |
| 7 | 자격 | 28 | 결제상태 | 49 | 현금영수증 |
| 8 | 제품코드 | 29 | 결제수단 | 50 | 현금영수증번호 |
| 9 | 병원명 | 30 | 상병코드 | 51 | 신환Master 등록일 |
| 10 | 구분(SB/SCI) | 31 | 건보위임시작일 | 52 | 검수 담당자 |
| 11 | 상병 타입 | 32 | 건보위임종료일 | 53 | 수정 담당자 |
| 12 | 모든서류 발행일 | 33 | 환산기간 | 54 | System Operator |
| 13 | 환급 해당기관 | 34 | 요양병원코드 | 55 | 제품코드 *(중복)* |
| 14 | 총 처방기간 | 35 | 사유 | 56 | 수량 |
| 15 | 담당의사명 | 36 | 인마켓 마감일자 | 57 | 제품 관리번호(장비코드) |
| 16 | 다음재구매가능일 | 37 | 설명 | 58 | 검수 메모 |
| 17 | Five/Six(110days) | 38 | 등록담당자 | | |
| 18 | Five/Six Program | 39 | 수정담당자 | | |
| 19 | 송금자명 | 40 | 진단확인일 | | |
| 20 | 법정대리인 또는 가족 성명 | 41 | 상병 구분 | | |
| 21 | 법정대리인 또는 가족 생년월일 / 가입자ㆍ피부양자와의 관계 | 42 | — | | |

> **분석 메모**
> - `제품코드`가 **8번과 55번에 중복** 출현합니다 → 의도 확인 필요 (`Q-14`)
> - `Five/Six Program`은 T-11에 따라 `Five/Six`로 통일되어야 하는데 컬럼 목록에는 구 명칭이 남아 있습니다 → 표기 통일 확인 필요
> - **컬럼 58개를 한 화면에 가로 배치하는 것은 실사용이 어렵습니다.** 컬럼 선택(Column Chooser) + 사용자별 레이아웃 저장 기능을 권장합니다 → `Q-15`

---

### 3-17. 주문 관리 — 상세 내용 (회신 28p)

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-ORM-22` | `RN` | `건보 청구액` → **기관 부담금** (T-07) | UI | 小 | ✅ 라벨 변경 |
| `CR-ORM-23` | `RN` | `환자 본인부담` → **본인부담금** (T-06) | UI | 小 | ✅ 라벨 변경 |
| `CR-ORM-24` | `AD` | **유형** 포함 | UI+API | 中 | ⚠ 유형 `NEW` |
| `CR-ORM-25` | `AD` | **자격** 포함 | UI+API | 小 | ✅ `counseling_data.udf11` 존재 |
| `CR-ORM-26` | `AD` | **비율** 포함 | UI+API | 小 | ⚠ `CR-ORI-12` 매트릭스 확정 후 |

---

### 3-18. 현황 Dashboard (회신 29~30p)

**화면 코드: `DSH` · 대폭 확장 요청**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-DSH-01` | `FN` | **월 단위 집계 대시보드 신설** — 집계 항목 아래 | UI+API+집계로직 | 大 | ⚠ 월별 집계 로직 신설. 14개 항목 중 `조기/정상/지연구매` 는 판정 기준 미정의(`Q-17`) |
| `CR-DSH-02` | `FN` | **청구 상태별 건수 표시** — 대기 / 진행중 / 완료 / 반려 / 재신청 / 취소 / 지급 완료 | UI+API | 中 | ⚠ `orders.nhis_claim_status` 존재하나 요청 7종과 매핑 규칙 필요 |
| `CR-DSH-03` | `Q` | **반려 사유 코드** — 추후 공유 예정 | — | — | → `Q-16` |

**`CR-DSH-01` 월별 집계 항목 (회신 29p)**

| # | 집계 항목 | # | 집계 항목 |
|---|---|---|---|
| 1 | 유형 | 8 | 조기구매 / 정상구매 / 지연구매 |
| 2 | 신규 환자 등록 수 | 9 | 조기·정상·지연구매 **사유** |
| 3 | 신구매 / 재구매 | 10 | 현금영수증 발행 건수 |
| 4 | 주문 건수 | 11 | 전자세금계산서 발행 건수 |
| 5 | 대상월 | 12 | 청구 수 |
| 6 | 주문 확정 월 | 13 | 청구 상태 |
| 7 | 주문상태 | 14 | 청구 사유 |

> **신규 개념 발견** — `조기구매 / 정상구매 / 지연구매` 및 그 **사유** 는 기존 URS·FRS에 정의되지 않은 개념입니다. 판정 기준(재구매 가능일 대비 실제 주문일 차이 등)을 정의해야 합니다. → `Q-17`

---

### 3-19. 재구매 관리 (회신 31p)

**화면 코드: `REP`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-REP-01` | `RN` | `재구매 예정일` → **재구매 가능일** (T-20) | UI | 小 | ✅ 라벨 변경 |
| `CR-REP-02` | `AD` | **Five/Six(110days)** 추가 | UI+API | 小 | ✅ `counseling_data.five` 존재 |
| `CR-REP-03` | `AD` | **Five/Six** 추가 | UI+API | 小 | ✅ `counseling_data.five_program` 존재 |

---

### 3-20. 서류 관리 (회신 32~36p)

**화면 코드: `DOC`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-DOC-01` | `FN` | 표시 건수 제한 해제 (GL-02) | UI+API | — | ⚠ `Q-01` 선행 |
| `CR-DOC-02` | `NEW` | **거래명세서** 서식 추가 *(Supportworks에 파일 업로드되어 있음)* | UI+API+서식 | 中 | ❓ 서식 파일 미수령(`D-01`) |
| `CR-DOC-03` | `NEW` | **의료용품 구입확인서** 서식 추가 *(Supportworks에 파일 업로드되어 있음)* | UI+API+서식 | 中 | ❓ 서식 파일 미수령(`D-02`) |
| `CR-DOC-04` | `NEW` | **건강보험공단 요양비 지급 청구서** 서식 추가 *(Supportworks에 파일 업로드, 요양기관정보마당 화면에서 입력·청구 가능)* | UI+API+서식 | 中 | ❓ 서식 파일 미수령(`D-03`) |
| `CR-DOC-05` | `NEW` | **기초(시군구청) 요양비 지급 청구서** 서식 추가 *(Supportworks에 파일 업로드되어 있음)* | UI+API+서식 | 中 | ❓ 서식 파일 미수령(`D-04`) |
| `CR-DOC-06` | `AD` | 조회결과 탭에 **유형 / 주민등록번호 / 병원** 표시 추가 | UI+API | 小 | ✅ `prescription_documents` → `prescriptions` 조인으로 주민번호·병원명 표시 가능 |
| `CR-DOC-07` | `Q` | **의료용품 구입확인서** — 모든 유형 포함, **교환/반품/취소 건 포함 여부 확인 필요** | — | — | → `Q-18` |

> **선행 조치** — `CR-DOC-02` ~ `CR-DOC-05`는 **Supportworks에 업로드된 실제 서식 파일 4종을 수령**해야 개발 착수가 가능합니다. §7 자료 요청 목록 `D-01` ~ `D-04`.

---

### 3-21. 위임장 서명 (회신 37p)

**화면 코드: `POA`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-POA-01` | `RN` | T-01 적용 — 법정대리인 또는 가족 성명 | UI | 小 | ✅ 라벨 변경 (`prescription_consents.guardian_name`) |
| `CR-POA-02` | `RN` | T-02 적용 — 법정대리인 또는 가족 생년월일 | UI | 小 | ✅ 라벨 변경 (`guardian_birth_date`) |
| `CR-POA-03` | `RN` | T-03 적용 — 가입자ㆍ피부양자와의 관계 | UI | 小 | ✅ 라벨 변경 (`guardian_relation`) |

---

### 3-22. 개인정보 동의 (회신 38p)

**화면 코드: `PRV`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-PRV-01` | `RN` | `카테터` → **IC** (T-22) | UI | 小 | ✅ 라벨 변경 (`privacy_consents.type = catheter`) |
| `CR-PRV-02` | `RN` | `장루` → **OC** (T-23) | UI | 小 | ✅ 라벨 변경 (`type = stoma`) |
| `CR-PRV-03` | `AD` | **관리자 메모** 추가 | UI+DB+API | 小 | ⚠ `privacy_consents` 에 메모 컬럼 없음 → DDL 1 |

---

### 3-23. 채팅 (회신 39p)

**화면 코드: `CHT`**

| ID | 유형 | 요청 내용 | 영향 | 작업량 | VS Code 확인 결과 |
|---|---|---|---|---|---|
| `CR-CHT-01` | `FN` | **답장 표시 위치 변경** — 원 대화 아래가 아니라 **카카오톡처럼 최하단 라인에 표시** (인용 형태) | UI+FN | 中 | ⚠ `chat_messages` 에 `reply_to` 없음 → DDL 1 + 렌더 변경 |
| `CR-CHT-02` | `FN` | **확인(읽음) 태그 표시** — 카카오톡처럼 `v` 등의 표기 | UI+DB+FN | 中 | ⚠ 읽음 상태를 담는 컬럼·표가 없음 → 신설 필요 |

---

### 3-24. 【신규】 Finance 항목 (회신 40~45p)

**화면 코드: `FIN` · 유형 `NEW` · ⚠ CR 대상 · 6개 화면**

#### 공통 확인사항 (회신 40p)

| ID | 유형 | 요청 내용 | 영향 |
|---|---|---|---|
| `CR-FIN-00a` | `FN` | **모든 메뉴 엑셀 다운로드 가능** (GL-06) | UI+API |
| `CR-FIN-00b` | `FN` | **주문번호 기준으로 메뉴 간 데이터 연결 가능** | 데이터모델 |
| `CR-FIN-00c` | `FN` | **조회기간 조건 검색 (일별 / 월별)** | UI+API |

#### `FIN-1` 통합주문내역 (40p) — 목적: 전체 주문 현황 및 매출 확인

| # | 항목 | # | 항목 |
|---|---|---|---|
| 1 | 주문번호 | 10 | 배송일자 |
| 2 | 주문일자 | 11 | 배송상태 |
| 3 | 환자ID | 12 | 송장번호 |
| 4 | 환자명 | 13 | 주문상태 |
| 5 | 제품코드 | 14 | 취소여부 |
| 6 | 제품명 | 15 | 취소일자 |
| 7 | 주문수량 | | |
| 8 | 주문금액 | | |
| 9 | 환자부담금(10%) / 공단·지자체 부담금(90%) | | |

#### `FIN-2` 정산내역 — 환자결제(10%) (41p) — 목적: 환자 본인부담금 입금 확인

| # | 항목 | # | 항목 |
|---|---|---|---|
| 1 | 주문번호 | 9 | **PG사** ※ PG사 관련 항목 추가 필요 |
| 2 | 주문일자 | 10 | PG정산일 |
| 3 | 환자명 | 11 | PG정산금액 |
| 4 | 주문금액 | 12 | PG수수료 |
| 5 | 본인부담액 | 13 | 회사계좌입금일자 |
| 6 | 입금일자 / 입금금액 / 입금자명 | 14 | 회사계좌입금금액 |
| 7 | 결제수단 | 15 | 정산상태 |
| 8 | 정산상태 | | |

#### `FIN-3` 공단·지자체(90%) (42p) — 목적: 공단 및 지자체 지급금 관리

| # | 항목 | # | 항목 |
|---|---|---|---|
| 1 | 주문번호 / 주문일자 / 환자명 | 7 | 비고 |
| 2 | 지급기관명 | 8 | PG사 / PG정산일 / PG정산금액 / PG수수료 |
| 3 | 청구금액 | 9 | 회사계좌입금일자 / 회사계좌입금금액 |
| 4 | 승인금액 | 10 | 정산상태 |
| 5 | 입금일자 / 입금금액 | | |
| 6 | 정산상태 / 미정산금액 | | |

#### `FIN-4` 미정산내역 (43p) — 목적: 미수금 관리

주문번호 / 주문일자 / 환자명 / 총 주문금액 / 환자부담금 / 공단부담금 / 실제 수납금액 / 미정산금액 / **미정산구분(환자·공단·지자체)** / 경과일수 / 조치상태

#### `FIN-5` 반품환불내역 (44p) — 목적: 매출 차감 및 환불 관리

주문번호 / 환자명 / 제품명 / 반품접수일 / 반품완료일 / 반품수량 / 반품금액 / 환불일자 / 환불금액 / 환불상태 / 사유

#### `FIN-6` 부가세신고내역 (45p) — 목적: 부가세 신고자료 생성 및 검증

주문일자 / 주문번호 / 주문상태 / 배송일자 / 환자명 / 제품명 / 공급가액 / 부가세 / 합계금액 / **결제별 구분금액(카드·현금영수증·세금계산서)**

> **Finance 모듈 분석 의견**
> 1. `FIN-2` · `FIN-3` 양쪽에 **PG 관련 항목**(PG사·정산일·정산금액·수수료)이 요청되어 있습니다. 공단·지자체 지급금은 PG를 경유하지 않으므로 `FIN-3`의 PG 항목은 **요청 의도 확인이 필요**합니다. → `Q-19`
> 2. `FIN-2`에 `정산상태`가 **2회 중복** 기재되어 있습니다(6행·15행). 두 개가 서로 다른 상태(PG 정산상태 vs 회사 입금 정산상태)인지 확인 필요. → `Q-20`
> 3. 6개 화면 모두 **엑셀 다운로드 + 주문번호 기준 연결 + 기간 조회**가 공통 요건이므로, 개별 화면이 아닌 **공통 리포트 프레임워크**로 구현하는 것이 효율적입니다.
> 4. **Toss Payments 정산 API 연동 범위 확대**가 필요합니다 (PG정산일·정산금액·수수료 수신).

---

## 4. 향후 추가 공유 예정 항목 (콜로 명시)

| ID | 항목 | 출처 | 상태 |
|---|---|---|---|
| `P-01` | 일부 화면에 대한 추가 회신 | 2p | 대기 |
| `P-02` | 자동차보험 자격의 비율·현금영수증·세금계산서 규칙 | 20p | 대기 |
| `P-03` | 산재 자격의 부가세 발행 방식 | 20p | 확인중 |
| `P-04` | 청구 반려 사유 코드 | 30p | 대기 |
| `P-05` | 제품 관리번호(장비코드) 엑셀 파일 | 27p | Stella 연락 시 제공 |

---

## 5. 링크더랩 → 콜로플라스트 요청 자료

| ID | 자료 | 출처 | 요청 대상 | 용도 |
|---|---|---|---|---|
| `D-01` | 거래명세서 서식 파일 | Supportworks | 콜로플라스트 | `CR-DOC-02` |
| `D-02` | 의료용품 구입확인서 서식 파일 | Supportworks | 콜로플라스트 | `CR-DOC-03` |
| `D-03` | 건강보험공단 요양비 지급 청구서 서식 파일 | Supportworks | 콜로플라스트 | `CR-DOC-04` |
| `D-04` | 기초(시군구청) 요양비 지급 청구서 서식 파일 | Supportworks | 콜로플라스트 | `CR-DOC-05` |
| `D-05` | 제품 관리번호(장비코드) 엑셀 | Supportworks | Stella | `CR-ORM-20` |
| `D-06` | 위임장 양식 원본 (용어 확정 근거) | — | 콜로플라스트 | T-01~T-04 |
| `D-07` | 위드웍스 `보호자명` 필드 사양 (송금자명 연결용) | WITHWORKS | 콜로플라스트/WW | `CR-CUS-12`, `CR-ORP-07` |
| `D-08` | 위드웍스 `판매상태 2` 코드 정의서 | WITHWORKS | 콜로플라스트/WW | `CR-ORM-03`, `Q-13` |
| `D-09` | 위드웍스 `청구전략` 코드 정의서 (비율 매핑 근거) | WITHWORKS | 콜로플라스트/WW | `CR-ORI-12` |
| `D-10` | 위드웍스 CE 샘플판매주문 화면·API 사양 | WITHWORKS | 콜로플라스트/WW | `CR-SMP-01` |

---

## 6. 확인 필요 항목

### 6-1. 콜로플라스트 회신 필요 (Q)

| ID | 관련 | 질문 | 선택지 / 링크더랩 의견 |
|---|---|---|---|
| `Q-01` | `GL-02` | 목록 "전체 표시" 요청에 대해, 성능을 고려한 대안 중 어느 방식을 택하시겠습니까? | A) 가상 스크롤(권장) B) 기본 500건+전체조회 버튼 C) 페이지 크기 선택 저장 |
| `Q-02` | `T-25` | `거래처관리` 메뉴 명칭을 **Master** 로 변경하는 건이 대표님 확인 완료되었습니까? 확정 명칭은 무엇입니까? | 미확정 시 코드 반영 보류 |
| `Q-03` | `CR-CUS-17` | 생년월일(1)/(2)/생년 3개를 **각각 독립 입력·수정** 하시겠습니까, 아니면 **1개 입력 → 3가지 표기 자동 생성** 이면 되겠습니까? | 후자 권장 (정합성) |
| `Q-04` | `CR-RXL-06` | 처방전 목록의 `OCR 처리중`이 `대기중`과 동일한 의미입니까? | 동일하면 3종 상태로 통합 |
| `Q-05` | `CR-MNU-02~04` | 메뉴 그룹 재배치로 기존 확인서(LTL-UNICORN-CFM-2026-0812-01)의 28개 메뉴 구조가 변경됩니다. **확인서 재발행**이 필요합니까? | |
| `Q-06` | `CR-ORP-18` | 주문 화면 환자정보에서 `서명여부`·`미성년`·`법정대리인 또는 가족 신분증`·`등록자/수정자/수정일자` 가 제외된 것은 **의도된 차이**입니까? | |
| `Q-07` | `CR-ORI-02`, `CR-ORM-16` | `판매 유형` / `주문유형` 항목이 필요합니까? | 불필요 시 제거 |
| `Q-08` | `CR-ORI-09`, `CR-ORL-01` | **주문 정보 탭과 주문 연계 탭을 통합**하시겠습니까? | 통합 시 화면 재설계 필요 |
| `Q-09` | `CR-ORI-10/11` | **Lot Number·유효기간** 관리를 WITHWORKS WMS가 지원합니까? UNICORN이 표시만 하면 됩니까, 관리까지 해야 합니까? | 물류연계 문서 미확정 항목과 동일 |
| `Q-10` | `CR-ORI-12` | **산재** 자격의 부가세 발행 방식 확정 시점은 언제입니까? | 확정 전까지 선택 비활성화 제안 |
| `Q-11` | `CR-ORI-12` | **자동차보험** 자격의 비율·현금영수증·세금계산서 규칙 확정 시점은 언제입니까? | 동상 |
| `Q-12` | `CR-RTN-12` | 교환·환불 프로세스에서 **승인 단계를 어느 위치**에 두시겠습니까? | 검수중 이후 권장 |
| `Q-13` | `CR-ORM-03` | 주문관리 `상태` 가 위드웍스 `판매상태 2` 와 동일 의미입니까? 동일하면 어떤 명칭으로 통일합니까? | |
| `Q-14` | `CR-ORM-21` | 조회 결과 컬럼 목록에 **`제품코드`가 2회** 나타납니다. 의도된 것입니까? | |
| `Q-15` | `CR-ORM-21` | 58개 컬럼 전체 상시 노출 대신 **컬럼 선택 기능 + 사용자별 저장**을 도입해도 되겠습니까? | 권장 |
| `Q-16` | `CR-DSH-03` | 청구 **반려 사유 코드** 공유 시점은 언제입니까? | |
| `Q-17` | `CR-DSH-01` | **조기구매 / 정상구매 / 지연구매**의 판정 기준과 사유 코드를 정의해 주십시오. | URS 미정의 신규 개념 |
| `Q-18` | `CR-DOC-07` | 의료용품 구입확인서에 **교환/반품/취소 건을 포함**합니까? | |
| `Q-19` | `FIN-3` | 공단·지자체(90%) 내역에 **PG 관련 항목**이 필요한 이유를 확인 부탁드립니다. | 공단 지급은 PG 미경유 |
| `Q-20` | `FIN-2` | 정산내역의 **`정산상태` 2개**는 서로 다른 상태입니까? (PG 정산상태 / 회사 입금 정산상태) | |

### 6-2. 링크더랩 답변 필요 (A) — 콜로가 우리에게 질문한 항목

| ID | 출처 | 콜로 질문 | 답변 준비 상태 |
|---|---|---|---|
| `A-01` | 14p | **"판매상태 2 확정일 때, Master 화면에서 update 및 해당 정보가 가장 최근 오더 라인에 연결되어야 하는데(위드웍스처럼) 가능한지"** — 현재는 Stella가 위드웍스 거래처추가정보에서 `환자거래처 정보수정` 버튼을 누르면 **가장 최근 오더 라인에만 연결**(기존 라인은 연결 불필요) | **VS Code 확인 후 답변** — §8-3 |
| `A-02` | 9p | `OCR 처리중` 상태의 현재 시스템상 정의 설명 | VS Code 확인 후 답변 |
| `A-03` | 26p | `상태` 필드의 현재 정의 및 위드웍스 판매상태 2와의 매핑 가능 여부 | VS Code 확인 후 답변 |

> **`A-01`은 이번 회신에서 콜로가 명시적으로 답변을 요구한 유일한 항목**입니다. 회신 PPT에 반드시 답변을 포함해야 합니다.

---

## 7. 우선순위 및 처리 순서 제안

| 순위 | 그룹 | 내용 | 근거 |
|---|---|---|---|
| **P0** | 선행 확정 | `Q-01`(목록 정책) · `Q-02`(Master 명칭) · `Q-08`(탭 통합) · `A-01` 답변 | 이 4건이 확정되지 않으면 화면 구조 자체가 흔들려 재작업 발생 |
| **P1** | 전역 공통 | `GL-01`(3열) · `GL-03`(용어사전) · `GL-04`(감사항목) · `GL-05`(IC/OC 표기) | 개별 화면보다 먼저 처리해야 중복 작업 방지 |
| **P2** | 데이터 모델 | `CR-ORP-01`(Master 참조 구조) · 신규 필드 일괄 추가 · `CR-ORI-12`(비율 매트릭스) | 스키마 변경은 조기 확정 필요 |
| **P3** | 기존 화면 수정 | §3-1 ~ §3-23 개별 항목 | 화면 단위 병렬 진행 가능 |
| **P4** | CR 대상 신규 | 반품/교환/취소 · Finance 6종 · Dashboard 확장 | 별도 CR 승인 후 착수 |
| **P5** | 보류 | 산재·자동차보험 비율 · 반려 사유 · 추가 공유 예정 항목 | 콜로 회신 대기 |

---

## 8. VS Code 작업 지시

> **아래 지시는 STEP 2에서 VS Code(Claude Code)가 수행합니다.**

### 8-1. 공통 원칙

1. **코드를 변경하지 말 것.** 이 단계는 **실물 대조 및 실현 가능성 판정**만 수행합니다.
2. 모든 판정 결과는 본 MD 파일의 `VS Code 확인 결과` 컬럼에 기록합니다.
3. **근거 없는 추정 금지** — 코드/스키마에서 확인되지 않은 항목은 `미확인`으로 표기합니다.
4. 판정 기호: `✅ 반영 가능(단순)` / `⚠ 반영 가능(설계 변경 수반)` / `❌ 현 구조에서 불가` / `❓ 미확인`

### 8-2. 필드 존재 여부 대조 작업

다음 4개 필드 목록에 대해 **현재 DB 스키마 및 API 응답 모델과 1:1 대조**하고, 각 필드를 아래 3가지로 분류합니다.

| 대상 목록 | 필드 수 | 위치 |
|---|---|---|
| 거래처관리 상세 필드 | 34 | §3-1 `CR-CUS-18` |
| 주문 환자정보 필드 | 30 | §3-8 `CR-ORP-18` |
| 병원 처방정보 필드 | 32 | §3-9 `CR-ORH-12` |
| 주문관리 조회결과 컬럼 | 58 | §3-16 `CR-ORM-21` |

**분류 기준**

| 분류 | 의미 | 후속 조치 |
|---|---|---|
| `EXISTS` | 동일 의미 컬럼이 이미 존재 | 라벨만 변경 |
| `RENAME` | 다른 이름으로 존재 | 매핑표 작성 |
| `NEW` | 존재하지 않음 | **DDL 작성 필요 · 작업량 산정** |

**산출물** — 필드 대조표 (`필드명 / 분류 / 현재 컬럼명 / 테이블 / 타입 / 비고`)

### 8-3. `A-01` 답변을 위한 기술 검토

콜로 질문: *"판매상태 2 확정 시 Master 화면 업데이트 내용이 가장 최근 오더 라인에 연결 가능한가"*

**확인 항목**

1. 현재 주문 라인이 환자 마스터를 **참조(FK)** 하는지, **값 복사(스냅샷)** 하는지
2. 판매상태 2에 해당하는 상태 값이 UNICORN에 존재하는지, WITHWORKS 연동 필드에 있는지
3. "가장 최근 오더 라인"을 식별할 수 있는 정렬 기준(주문일시·주문번호 채번 규칙)이 있는지
4. 마스터 수정 시 **최근 1건만 갱신하고 과거 라인은 보존**하는 처리가 현 구조에서 가능한지
5. 불가하다면 필요한 구조 변경 범위와 작업량

**산출물** — 기술 검토 의견서 (가능/불가 + 근거 + 대안 + 작업량)

### 8-4. 영향도 및 작업량 산정

| 산정 단위 | 기준 |
|---|---|
| `小` | 0.5일 이하 (라벨 변경, 단순 컬럼 추가) |
| `中` | 0.5 ~ 3일 (화면 재구성, 신규 필드 + API 수정) |
| `大` | 3일 초과 (데이터 모델 변경, 신규 모듈) |

**산출물** — 그룹별 합계 공수 및 §7 우선순위별 일정 영향 분석

### 8-5. 일정 영향 검토

| 확인 항목 | 기준선 |
|---|---|
| Phase 1 CE Admin 개발 완료 | **2026-10-02** |
| CE Admin 파일럿 개시 | **2026-10-05** |
| Go-Live | **2027-01-29** (계약 고정) |

- P1~P3 항목만 반영 시 기준선 준수 가능 여부
- P4(CR 대상) 반영 시 필요 추가 기간
- CR 없이는 불가능한 항목 명시

### 8-6. 본 MD 갱신 규칙

- 각 `CR-*` 행의 `VS Code 확인 결과` 컬럼에 판정 기호 + 한 줄 근거를 기입
- `작업량` 컬럼을 실측 기준으로 갱신
- §8-2 필드 대조표를 **§11 부록**으로 추가
- §8-3 기술 검토 의견서를 **§12 부록**으로 추가
- 신규 발견 사항은 `CR-{화면}-9x` 번호로 추가 (원본 회신에 없는 항목임을 명시)

---

## 9. CR(변경 요청) 판정

다음 항목은 **기존 URS / FRS / 확정 메뉴 28개 범위를 초과**할 가능성이 높습니다. 별도 CR 발행 후 진행 여부를 결정해야 합니다.

| CR 후보 | 내용 | 초과 근거 | 판정 |
|---|---|---|---|
| `CR-후보-1` | **반품/교환/취소 모듈** (§3-13) | 확정 메뉴 28개에 미포함, 3PL 연동·환불 프로세스 신규 | **CR 필요** |
| `CR-후보-2` | **Finance 6개 화면** (§3-24) | URS 미정의, PG 정산 API 연동 확장 수반 | **CR 필요** |
| `CR-후보-3` | **현황 Dashboard 대폭 확장** (§3-18) | 14개 집계 항목 + 조기/정상/지연 구매 신규 개념 | **CR 필요** |
| `CR-후보-4` | **처방전 업로드 10 → 40장** (§3-3) | Textract 처리량 4배, 비동기 전환 수반 | **CR 검토** |
| `CR-후보-5` | **환자 Master 구조 재정의** (§3-8 `CR-ORP-01`) | 데이터 모델 변경, 주문↔환자 참조 방식 변경 | **CR 검토** |
| `CR-후보-6` | **Lot / 유효기간 관리** (`CR-ORI-10/11`) | WITHWORKS 로트 추적 미확정 | **CR 검토** |
| `CR-후보-7` | **서류 4종 신규 서식** (§3-20) | 확정 서식 목록 대비 추가 여부 확인 필요 | **확인 후 판정** |

> **주의** — 위 판정은 링크더랩의 1차 의견이며, 확정된 URS·FRS 원문과 대조하여 최종 확정해야 합니다. 대조 결과 URS 범위 내로 확인되면 CR 없이 진행합니다.

---

## 10. Claude 웹 (Unicorn Project) PPT 생성 지침

> **STEP 3에서 사용** — 본 MD 갱신본을 Claude 웹에 전달하며 아래 지침을 함께 제시합니다.

### 10-1. 문서 목적

콜로플라스트에 **① 회신 내용을 정확히 이해했음을 확인받고 ② Q항목 20건에 대한 회신을 받고 ③ CR 대상 7건의 진행 여부를 결정받아** 개발에 착수하기 위한 확인요청서.

### 10-2. 슬라이드 구성

| # | 슬라이드 | 내용 | 출처 |
|---|---|---|---|
| 1 | 표지 | 화면 확정요청 1차 회신 검토 결과 및 재확인 요청 | — |
| 2 | 회신 접수 요약 | 190건 접수 · 유형별 분포 · 처리 방침 | §1-1, §1-2 |
| 3 | 핵심 구조 변경 3건 | S-1 환자 Master / S-2 반품·교환·취소 / S-3 Finance | §1-3 |
| 4 | 전역 공통 변경 | GL-01 ~ GL-06 | §2 |
| 5 | 용어 통일 사전 | T-01 ~ T-25 (표) | §2-2 |
| 6 | 목록 표시 정책 검토 의견 | 성능 이슈 + 대안 3안 → `Q-01` | §2-1 |
| 7~14 | 화면별 반영 계획 | 화면당 1장, 반영 항목 요약 + 반영 이미지 자리 | §3 |
| 15 | 【신규】 반품/교환/취소 | 상태 체계 3종 + 프로세스 | §3-13 |
| 16 | 【신규】 Finance 6종 | 화면별 목적·항목 요약 | §3-24 |
| 17 | 부담 비율 매트릭스 확인 | 6개 자격 × 비율 (산재·자동차보험 미확정 강조) | `CR-ORI-12` |
| 18 | **A-01 답변** | 판매상태 2 → 최근 오더 라인 연결 가능 여부 기술 답변 | §8-3 결과 |
| 19~20 | **확인 필요 항목 Q-01 ~ Q-20** | 2장 분할, 질문 + 링크더랩 권장안 + 회신란 | §6-1 |
| 21 | 요청 자료 D-01 ~ D-10 | 자료명 · 요청 대상 · 용도 | §5 |
| 22 | CR 판정 7건 | CR 필요/검토 구분 + 일정 영향 | §9 |
| 23 | 일정 영향 및 다음 단계 | 10-02 개발완료 / 10-05 파일럿 / 01-29 Go-Live 기준 영향 | §8-5 |

### 10-3. 디자인 및 톤

| 항목 | 지침 |
|---|---|
| 언어 | **한국어** (콜로플라스트 코리아 대상) |
| 폰트 | 맑은 고딕 (Malgun Gothic) |
| 색상 | 헤더 `#28798B` · 입력/회신란 `#FFF9DB` · 강조 네이비 계열 |
| 회신란 | Q항목 각 행에 **콜로 기입용 빈 칸** 배치 |
| 표기 원칙 | 회신 원문을 **그대로 인용**하고, 링크더랩 의견은 별도 컬럼으로 명확히 구분 |
| 금지 | 콜로가 명시하지 않은 내용의 임의 추가·추정 표기 |

### 10-4. 생성 시 유의사항

1. **Q항목 20건은 반드시 전부 포함** — 하나라도 누락되면 재확인 사이클이 한 번 더 발생합니다.
2. **A-01 답변은 단독 슬라이드**로 배치 — 콜로가 명시적으로 요청한 유일한 질문입니다.
3. CR 판정 슬라이드에는 **비용·일정 영향**을 함께 표기 (승인 판단 근거).
4. 화면별 슬라이드에는 **수정 후 화면 캡처 자리(placeholder)** 를 두어, 개발 반영 후 이미지를 채워 재발송할 수 있도록 합니다.

---

## 변경 이력

| 버전 | 일자 | 작성 | 내용 |
|---|---|---|---|
| v0.1 | 2026-08-13 | 링크더랩 | 콜로플라스트 화면 확정요청 1차 회신(45장) 분석 및 변경 사양 초안 |
| v0.2 | 2026-08-13 | VS Code (Claude Code) | STEP 2 — 실물 대조: §11 필드 대조표 · §12 A-01 기술 검토 · §13 종합 의견 · §14 판정 분포. 155개 CR 행 판정 기입 완료. §8-4 공수·§8-5 일정은 범위 제외 |

---

## 11. 부록 — VS Code 필드 대조표 (STEP 2 산출물)

> 대조 일시 2026-08-13 · 대조 대상: 운영 DB(`colo_unicorn`) 실제 스키마 + 소스코드
> 판정 기호 — `✅ 반영 가능(단순)` / `⚠ 반영 가능(설계 변경 수반)` / `❌ 현 구조에서 불가` / `❓ 미확인`

### 11-0. 대조에 앞서 — 이번 회신의 실제 난이도를 결정하는 사실

**요청된 필드 대부분은 이미 존재합니다. 다만 `patients`(환자 마스터)가 아니라
`prescriptions.counseling_data` JSON 안에, 처방전 1건 단위로 있습니다.**

현재 스키마는 이렇습니다.

| 표 | 성격 | 관련 컬럼 |
|---|---|---|
| `patients` | 환자 마스터 — **21개 컬럼뿐** | `name, resident_no_enc/_hash/_masked, birth_date, gender, mobile, phone, address, health_insurance_no, note, memo, is_nhis_eligible, nhis_coverage_rate` + RRN 보존기간 4개 |
| `prescriptions.counseling_data` | **JSON · 처방전 단위** | 상담·환자·병원·급여·거래 항목 **약 45종** (`udf2`~`udf43`, `guardian_*`, `nhis_*`, `five_*` 등) |
| `orders` | 주문 단위 | 금액·배송·위드웍스·세금계산서·현금영수증 |

즉 콜로가 "거래처관리(Master)에 있어야 한다"고 지목한 항목들(건보등록·건보위임동의 시작/종료일·
기초재평가·Five/Six·상병타입 등)은 **값은 이미 쌓이고 있지만 환자가 아니라 처방전에 매달려
있습니다.** 그래서 같은 환자의 처방전이 2건이면 값이 2벌 생기고, 서로 다를 수 있습니다.

> **결론** — `CR-ORP-01`(주문 화면은 Master 참조·편집 Block)은 **UI 작업이 아니라 데이터
> 이관 작업**입니다. `counseling_data` 의 환자성 항목을 `patients` 로 승격시키고, 기존 처방전
> 값을 이관(backfill)한 뒤, 주문 화면을 읽기 전용으로 바꾸는 순서가 되어야 합니다.
> 이는 §9 `CR-후보-5` 를 **CR 검토 → CR 필요** 로 올려야 할 근거입니다.

### 11-1. 거래처관리 상세 필드 (`CR-CUS-18`)

| # | 요청 필드 | 분류 | 현재 위치 | 비고 |
|---|---|---|---|---|
| 1 | Active/Inactive | `NEW` | — | `patients` 에 활성 플래그 없음 (`deleted_at` 은 삭제용) |
| 2 | IC/OC | `NEW` | (`privacy_consents.type` 에 catheter/stoma 존재) | 환자 마스터에는 없음 · `GL-05` 표기 규칙의 근거 필드 |
| 3 | 환자명 | `EXISTS` | `patients.name` | |
| 4 | 구분(SB/SCI) | `RENAME` | `counseling_data.udf6` | **처방전 단위 → 환자로 승격 필요** |
| 5 | 상병 타입 | `NEW` | — | `disease_code`(상병코드)와 다른 개념 |
| 6 | 주민등록번호 | `EXISTS` | `patients.resident_no_enc` / `_masked` | 암호화 저장(P0-1) |
| 7 | 생년월일(1) | `EXISTS(파생)` | `patients.birth_date` | 포맷만 다름 → `Q-03` 은 "1개 저장" 권고가 타당 |
| 8 | 생년월일(2) | `EXISTS(파생)` | 동상 | |
| 9 | 생년 | `EXISTS(파생)` | 동상 | |
| 10 | 성별 | `EXISTS` | `patients.gender` | **주의 — 현재 값이 비어 있음.** 주민번호에서 채우지 않는다 |
| 11 | Email | `RENAME` | `counseling_data.email` | 환자로 승격 필요 |
| 12 | Fax | `NEW` | — | |
| 13 | 주소 | `EXISTS` | `patients.address` | |
| 14 | 전화번호1 | `EXISTS` | `patients.mobile` | |
| 15 | 전화번호2 | `EXISTS` | `patients.phone` (또는 `counseling_data.mobile2`) | 두 곳에 있어 **정본 지정 필요** |
| 16 | 송금자명 | `NEW` | — | 위드웍스 보호자명 연결 → `D-07` 수령 후 확정 |
| 17 | 현금영수증 | `RENAME` | `orders.cash_receipt_type` | **주문 단위** — 환자 기본값으로 둘지 확인 필요 |
| 18 | 현금영수증번호 | `RENAME` | `orders.cash_receipt_no` · `counseling_data.udf23` | 두 곳 중복 |
| 19 | 서명여부 | `EXISTS(파생)` | `prescription_consents.status = agreed` | 거래처 그리드에 이미 표시 중 |
| 20 | 미성년 | `EXISTS(파생)` | `resident_no_masked` 앞 7자리로 판정 | 이미 구현(`ResidentNo::isMinorByMasked`) |
| 21 | 법정대리인 또는 가족 성명 | `RENAME` | `prescription_consents.guardian_name` · `counseling_data.guardian_name` | 라벨만 변경 |
| 22 | 법정대리인 또는 가족 생년월일 | `RENAME` | `prescription_consents.guardian_birth_date` | 동상 |
| 23 | 가입자·피부양자와의 관계 | `RENAME` | `prescription_consents.guardian_relation` | 동상 |
| 24 | 법정대리인 또는 가족 신분증 | `RENAME` | `prescription_consents.guardian_id_path` | 동상 |
| 25 | 참고 사항 | `RENAME` | `patients.note` / `patients.memo` | 두 컬럼 공존 — 용도 정리 필요 |
| 26 | 건보등록 | `RENAME` | `counseling_data.udf19` | 환자로 승격 필요 |
| 27 | 건보등록일 | `RENAME` | `counseling_data.nhis_reg_date` | 동상 |
| 28 | 건보재등록 대상자 | `RENAME` | `counseling_data.udf4` | 동상 |
| 29 | 건보 재등록 기한 | `RENAME` | `counseling_data.nhis_renew_due` | 동상 |
| 30 | 건보위임동의시작일 | `RENAME` | `counseling_data.udf42` | 동상 |
| 31 | 건보위임동의종료일 | `RENAME` | `counseling_data.udf43` | 동상 · 위임장 PDF 계산과 정합 확인 필요 |
| 32 | 기초(의료급여)재평가대상자 | `RENAME` | `counseling_data.basic_reeval` | 동상 |
| 33 | 기초(의료급여)재평가기한 | `RENAME` | `counseling_data.basic_reeval_due` | 동상 |
| 34 | 등록자 | `NEW` | — | `patients` 에 `created_by` 없음 (`GL-04`) |
| 35 | 수정자 | `NEW` | — | 동상 (`prescriptions` 에는 `updated_by` 있음) |
| 36 | 수정일자 | `EXISTS` | `patients.updated_at` | |
| 37 | 신환 master 등록일 | `RENAME` | `counseling_data.udf32` / `patients.created_at` | 두 의미가 다를 수 있음 — 확인 필요 |

**집계** — `EXISTS` 10 · `RENAME` 17 · `NEW` 7 · 그중 **`counseling_data` → `patients` 승격 대상 15건**

### 11-2. 주문 화면 환자정보 (`CR-ORP-18`)

11-1 과 **동일 필드 집합**에서 `서명여부`·`미성년`·`법정대리인 신분증`·`등록자/수정자/수정일자`
6개가 빠진 구성입니다(→ `Q-06`). 따라서 **추가 DDL 은 발생하지 않습니다.**

다만 `CR-ORP-01`(편집 Block)이 붙으면 성격이 달라집니다.

| 항목 | 현재 구현 | 판정 |
|---|---|---|
| 환자 정보 편집 | 검수 화면에서 **직접 수정 가능** — `POST /prescriptions/{rx}/ocr` 가 `patient_name_ocr`·`mobile_ocr`·`address_ocr`·`resident_no_ocr` 를 처방전에 쓴다 | ⚠ 설계 변경 수반 |
| 주문 → 마스터 반영 | `linkOrCreatePatient()` 가 **빈 칸만** 채운다. 이미 값이 있으면 덮어쓰지 않는다 | ⚠ |
| 마스터 → 주문 반영 | **없음.** `PatientController@update` 는 `patients` 만 수정하고 처방전·주문에 전파하지 않는다 | ❌ 현 구조에서 불가 |

### 11-3. 병원 처방정보 (`CR-ORH-12`)

| 요청 필드 | 분류 | 현재 위치 |
|---|---|---|
| 검수 메모 | `RENAME` | `prescriptions.review_memo` (읽기전용 연결은 `NEW`) |
| 설명 | `NEW` | — |
| 요양병원코드 | `RENAME` | `prescriptions.hospital_code` · `counseling_data.erp_cd9` (**중복**) |
| 병원명 | `EXISTS` | `prescriptions.hospital_name` |
| 판매거래처 | `NEW` | — (`counseling_data.dealer_type` 은 '유형'이라 의미가 다름) |
| 유형(원내/원외/처방외) | `NEW` | — |
| 신구매/재구매 | `RENAME` | `counseling_data.udf17` |
| 진단 확인일 | `RENAME` | `counseling_data.udf2` |
| 상병구분 | `RENAME` | `counseling_data.udf3` |
| 상병코드 | `EXISTS` | `prescriptions.disease_code` |
| 요류역학검사일 | `RENAME` | `counseling_data.udf7` |
| 자격 | `RENAME` | `counseling_data.udf11` · **`기타` 선택지 추가 필요** |
| 1일 처방개수 | `EXISTS` | `prescriptions.daily_count` |
| 총 처방기간 | `EXISTS` | `prescriptions.total_days` |
| 총계 | `EXISTS` | `prescriptions.total_count` |
| 처방전발행일 | `EXISTS` | `prescriptions.issued_date` |
| 처방전사용기간 | `RENAME` | `counseling_data.udf13` |
| 처방전종료일 | `RENAME` | `counseling_data.udf14` |
| 담당 의사명 | `EXISTS` | `prescriptions.doctor_name` |
| 진료과목 | `EXISTS` | `prescriptions.specialty` · `department` (**중복 2컬럼**) · 선택지 5종 고정은 `NEW` |
| 사유 | `RENAME` | `counseling_data.udf20` |
| 일일 도뇨횟수 | `RENAME` | `counseling_data.daily_use_qty` |
| Five/Six | `RENAME` | `counseling_data.five_program` |
| Five/Six(110days) | `RENAME` | `counseling_data.five` |
| 주문 담당자 | `RENAME` | `counseling_data.udf25` |
| System Operator | `NEW` | — |
| 환급해당기관 | `NEW` | — |
| 결제일 | `RENAME` | `counseling_data.pay_date` |
| 구입일(모든서류 발행일) | `RENAME` | `counseling_data.buy_date` |
| 사용 시작일 | `NEW` | — |
| 급여종료일(사용종료일) | `NEW` | — |
| 다음 재구매 가능일 | `RENAME` | `counseling_data.udf30` · `prescriptions.repurchase_date` (**중복**) |
| 등록담당자/등록일/수정자/수정일자 (`CR-ORH-06`) | `일부 EXISTS` | `prescriptions.created_by`·`updated_by`·`created_at`·`updated_at` 존재 · **화면 미노출** |

**집계** — `EXISTS` 8 · `RENAME` 17 · `NEW` 7 · **중복 컬럼 4쌍 정리 필요**
(`hospital_code`/`erp_cd9`, `specialty`/`department`, `repurchase_date`/`udf30`, `patients.phone`/`mobile2`)

### 11-4. 주문관리 조회결과 컬럼 (`CR-ORM-21`)

| 구분 | 건수 | 대표 항목 |
|---|---|---|
| `EXISTS` 그대로 있음 | 22 | 주문번호·환자명·주민등록번호·병원명·제품코드·수량·판매상태·출고상태·결제상태·현금영수증번호·상병코드·처방전발행일·총 처방기간·담당의사명 등 |
| `RENAME` 다른 이름으로 있음 | 24 | 자격(`udf11`)·구분SB/SCI(`udf6`)·건보위임시작/종료일(`udf42/43`)·요양병원코드(`erp_cd9`)·사유(`udf20`)·등록/수정담당자·진단확인일(`udf2`)·다음재구매가능일(`udf30`) 등 |
| `NEW` 없음 | 12 | 유형 · 상병 타입 · 판매 거래처명 · 판매상태2 · 환산기간 · 인마켓 마감일자(값은 있으나 화면 미노출) · System Operator · 마지막 확정수량 · 하루 사용수량 · 제품 관리번호(장비코드) · 신환Master 등록일 · 설명 |

> **`판매상태` / `판매상태2`** — CE Admin 은 위드웍스 `so_show` 에서 **상태를 하나만** 받습니다
> (`orders.withworks_status` / `withworks_status_label`). 배송 상태는 별도(`withworks_ship_status`)입니다.
> **판매상태2 에 해당하는 값은 현재 수신 자체가 없습니다** → `Q-13`·`A-03` 답변 근거.

---

## 12. 부록 — `A-01` 기술 검토 의견서 (STEP 2 산출물)

**콜로 질문** — *"판매상태 2 확정일 때, Master 화면에서 update 및 해당 정보가 가장 최근 오더 라인에
연결되어야 하는데(위드웍스처럼) 가능한지"*

### 12-1. 결론

**현 구조에서는 불가능합니다.** 부분 구현은 가능하나, 요청대로 동작시키려면 데이터 모델 변경이
필요합니다.

| 요구 | 판정 | 근거 |
|---|---|---|
| ① 주문 라인이 환자 마스터를 참조하는가 | **⚠ 혼합** | `orders.patient_id` · `prescriptions.patient_id` FK 가 있으나 **동시에 값을 복사해 둔다** — `prescriptions.patient_name_ocr`·`mobile_ocr`·`address_ocr`·`resident_no_ocr_enc`. 화면은 스냅샷을 보여준다 |
| ② 판매상태 2 값이 존재하는가 | **❌ 없음** | `so_show` 응답에서 `status`/`status_label` 하나만 받아 `orders.withworks_status(_label)` 에 저장한다 |
| ③ 가장 최근 오더 라인을 식별할 수 있는가 | **✅ 가능** | `orders.created_at`·`id`·`order_number` 로 정렬 가능. 환자 기준은 `orders.patient_id` |
| ④ 마스터 수정 시 최근 1건만 갱신·과거 보존 | **❌ 불가** | `PatientController@update` 는 `patients` 만 수정한다. 전파 코드가 없다 |
| ⑤ 역방향(주문 → 마스터) 반영 | **⚠ 제한적** | `linkOrCreatePatient()` 가 비어 있는 칸만 채운다 |

### 12-2. 지금 상태에서 실제로 일어나는 일

1. 검수 화면에서 환자 정보를 고치면 **그 처방전에만** 반영된다.
2. 같은 환자의 다른 처방전은 옛 값을 그대로 들고 있다.
3. 거래처관리에서 환자를 고쳐도 **어느 주문에도 반영되지 않는다.**
4. 결과적으로 같은 환자의 값이 처방전 수만큼 갈라진다.

### 12-3. 대안

| 안 | 내용 | 작업량 | 부작용 |
|---|---|---|---|
| **A (권장)** | 환자성 항목을 `patients` 로 승격(11-1 의 15건) → 주문 화면은 **참조·읽기전용**. 마스터 수정이 모든 라인에 자동 반영 | **大** 5~8일 (DDL + 이관 배치 + 화면 3종) | 과거 주문의 "당시 값"이 보존되지 않아 청구 근거 서류와 어긋날 수 있다 |
| **B (콜로 요청에 가장 가까움)** | A + **최근 오더 라인에만 스냅샷 갱신** 트리거. 과거 라인은 발행 당시 값 유지 | **大** A + 2~3일 | 위드웍스와 동일한 동작. 갱신 트리거(판매상태2 확정) 정의가 선행돼야 한다 |
| **C (단기)** | 마스터 수정 시 최근 주문 1건에 한해 **수동 '반영' 버튼** | **中** 1~2일 | 자동이 아니라 누락 가능 |

### 12-4. 답변 전 선행 확인

**B 안의 트리거는 "판매상태 2 확정"인데, CE Admin 은 판매상태 2 를 아직 수신하지 않습니다(위 ②).**
따라서 회신 순서는 이렇게 되어야 합니다.

1. 위드웍스 `so_show` 응답에 **판매상태 2 필드 추가**를 요청 (`D-08` 코드 정의서 포함)
2. 값이 들어와야 "확정 시점"을 판정할 수 있고, 그때 B 안이 성립한다
3. 그 전까지는 A 안(참조 구조 전환)만 선행 가능하다

### 12-5. `A-02` · `A-03` 답변 근거

| ID | 질문 | 코드 확인 결과 |
|---|---|---|
| `A-02` | `OCR 처리중` 의 정의 | `prescriptions.status` 는 7종 — `pending`/`ocr_processing`/`ocr_done`/`review_needed`/`approved`/`rejected`/`ordered`. `대기중`(`pending`)은 업로드 직후 OCR 이전, `ocr_processing` 은 Textract 처리 중이다. **동일하지 않다** → `Q-04` 는 "다르다"로 회신 |
| `A-03` | `상태` 의 현재 정의 | 주문 관리의 `상태` 는 `orders.status` 이며 실제 값은 **`pending`/`confirmed` 2종뿐**이다. 위드웍스 상태는 `orders.withworks_status_label` 에 따로 있다. **동일 의미가 아니다** |

---

## 13. STEP 2 종합 의견

### 13-1. 문서 추정치와 실측의 차이

| 항목 | 문서 추정 | 실측 | 근거 |
|---|---|---|---|
| `AD` 항목 추가 약 70건 | 작업량 大 | **실제 신규 DDL 은 약 26건** | 요청 필드 다수가 `counseling_data` JSON 에 이미 존재 |
| `CR-ORP-01` Master 참조 | 大 | **大 (유지)** — 단 UI 가 아니라 **데이터 이관** | §12 |
| `CR-ORM-21` 58컬럼 | 大 | **中~大** | 22개는 그대로, 24개는 JSON 에서 끌어오면 된다 |
| `CR-RXL-*` 처방전 목록 | 小 | **小 (유지)** | 컬럼 대부분 존재 |

### 13-2. 문서에 없지만 함께 정리해야 할 것

| ID | 발견 사항 | 근거 |
|---|---|---|
| `CR-SYS-91` | **중복 컬럼 4쌍** — `hospital_code`/`erp_cd9`, `specialty`/`department`, `repurchase_date`/`udf30`, `patients.phone`/`mobile2`. 용어 통일(`GL-03`) 전에 **정본을 먼저 정해야 한다** | 11-3 |
| `CR-SYS-92` | **`patients.gender` 가 비어 있다** — 주민번호에서 파생하지 않아 거래처관리 성별 칸이 전부 `-` 다. `CR-CUS-18` 10번이 화면에 나오려면 backfill 이 필요하다 | 2026-08-13 시연 보고서 P10 |
| `CR-SYS-93` | **`counseling_data` 는 JSON 이라 검색·정렬이 불가**하다. `CR-CUS-01~07`·`CR-ORM-04~15` 의 검색조건 추가 요청 대부분이 이 JSON 안 항목이다 | 11-1 · 11-4 |
| `CR-SYS-94` | `GL-02`(표시 건수 무제한) — 현재 목록은 서버 페이지네이션 + wwGrid 전량 렌더 구조다. 58컬럼 × 수만 건은 브라우저가 버티지 못한다. §2-1 대안 A(가상 스크롤)가 타당하다 | `Q-01` |

### 13-3. `CR-SYS-93` 이 일정에 미치는 영향

검색조건 추가만 모으면 `CR-CUS-01~07`(7건) + `CR-ORM-01,04~15`(13건) = **20건**이고,
그중 **14건이 `counseling_data` JSON 안**입니다. JSON 은 인덱스를 걸 수 없어 건수가 늘면
검색이 급격히 느려집니다.

> **권고** — 개별 화면 작업(P3) 이전에 **P2 단계에서 JSON→컬럼 승격을 한 번에** 처리해야 합니다.
> 화면별로 나눠 하면 이관 배치를 여러 번 돌게 되고, 그때마다 운영 데이터를 건드립니다.

### 13-4. 남은 STEP 2 작업

| 작업 | 상태 |
|---|---|
| §8-2 필드 대조 (4개 목록) | **완료** → §11 |
| §8-3 `A-01` 기술 검토 | **완료** → §12 |
| §8-6 각 `CR-*` 행 `VS Code 확인 결과` 기입 (155행) | **완료** → §3 각 표 · 분포는 §14 |
| §8-4 그룹별 합계 공수 | **이번 범위에서 제외** (별도 요청 시 산정) |
| §8-5 일정 영향 (10-02 / 10-05 / 01-29 기준선) | **이번 범위에서 제외** (동상) |

---

## 14. 부록 — 판정 분포 (§8-6 산출물)

155개 `CR-*` 행을 운영 DB 스키마·소스코드와 1:1 대조해 각 행의 `VS Code 확인 결과` 컬럼을
채웠습니다. 아래는 그 분포입니다.

| 판정 | 건수 | 의미 |
|---|---|---|
| `✅ 반영 가능(단순)` | 81 | 컬럼·값이 이미 있고 라벨·배치·조회만 손보면 된다 |
| `⚠ 반영 가능(설계 변경 수반)` | 53 | DDL 추가 · JSON→컬럼 승격 · 규칙 확정이 선행돼야 한다 |
| `❌ 현 구조에서 불가` | 15 | 모듈 자체가 없거나(반품·교환) 외부 사양이 미확정(로트·판매상태2) |
| `❓ 미확인` | 6 | 서식 파일·외부 사양 미수령 (`D-01`~`D-05` · `D-10`) |

> `❌` 15건 중 **11건이 반품/교환/취소 모듈**입니다. 이 모듈을 CR 로 분리하면 나머지는
> `❌` 4건(`CR-ORP-01` · `CR-ORI-10/11` · `CR-ORM-13`)으로 줄어듭니다.

### 14-1. 착수 전에 반드시 풀려야 하는 것

| 구분 | 항목 | 막고 있는 것 |
|---|---|---|
| **콜로 회신 대기** | `Q-01`(목록 정책) · `Q-02`(Master 명칭) · `Q-08`(탭 통합) | 화면 구조 자체가 흔들려 재작업이 발생한다 (§7 P0 판단이 타당) |
| **콜로 회신 대기** | `Q-09`(로트) · `Q-17`(조기/정상/지연 기준) | 해당 항목은 착수 자체가 불가능 |
| **자료 수령 대기** | `D-01`~`D-04`(서식 4종) · `D-05`(장비코드) · `D-07`(위드웍스 보호자명) · `D-08`(판매상태2) · `D-10`(샘플주문 사양) | `❓ 미확인` 7건이 여기 묶여 있다 |
| **선행 작업** | `counseling_data` JSON → 컬럼 승격 (`CR-SYS-93`) | 검색조건 추가 20건 중 **14건**이 동작하지 않는다 |
| **선행 작업** | 중복 컬럼 4쌍 정본 지정 (`CR-SYS-91`) | 용어 통일(`GL-03`) 후에도 화면마다 다른 값이 보인다 |

### 14-2. CR 판정 재검토 (§9 갱신 의견)

| CR 후보 | 문서 v0.1 | STEP 2 실측 후 | 근거 |
|---|---|---|---|
| `CR-후보-1` 반품/교환/취소 | CR 필요 | **CR 필요 (유지)** | 모듈·표·역물류 API 전부 없음 |
| `CR-후보-2` Finance 6종 | CR 필요 | **CR 필요 (유지)** | PG 정산 API 확장 수반 |
| `CR-후보-3` Dashboard 확장 | CR 필요 | **CR 필요 (유지)** | 신규 개념 미정의(`Q-17`) |
| `CR-후보-4` 업로드 10→40 | CR 검토 | **CR 필요로 상향** | 상수 변경이 아니라 동기→비동기 전환. Textract 처리량 4배 |
| `CR-후보-5` 환자 Master 재정의 | CR 검토 | **CR 필요로 상향** | UI 가 아니라 데이터 이관(§11-0·§12). 운영 데이터 backfill 포함 |
| `CR-후보-6` Lot/유효기간 | CR 검토 | **판정 보류 (유지)** | 위드웍스 WMS 지원 여부에 종속(`Q-09`) |
| `CR-후보-7` 서류 4종 | 확인 후 판정 | **CR 불필요 예상** | 기존 서류 생성 구조(`prescription_documents`) 재사용 가능. `D-01`~`D-04` 수령 후 확정 |
