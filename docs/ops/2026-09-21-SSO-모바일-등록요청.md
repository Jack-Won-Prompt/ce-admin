# 본사 요청 — CE Admin 모바일 앱 SSO 등록 (2026-09-21)

웹 SSO(Microsoft Entra ID)는 본사에서 보내 준 값으로 설정을 마쳤고 **로그인까지 확인했습니다.**
같은 앱 등록(App registration)에 **모바일 플랫폼을 추가**해 주시기를 요청드립니다.

대상 앱 등록: **[CP-SSO] CE Admin Dev/UAT (VV-0726844)**
Client ID `bff281d8-2fac-47eb-b832-d495f430fa7a` · Tenant ID `dbbb8b46-5ce8-4d5e-baf8-bda0d21dce29`

> Client Secret 은 웹 서버에만 보관합니다. 모바일 앱에는 넣지 않습니다(PKCE 사용).

---

## 1. 요청 내용

### 1-1. 플랫폼 추가 — iOS / macOS

| 항목 | 값 |
|---|---|
| Bundle ID | `com.coloplast.ceadmin` |
| Redirect URI | `msauth.com.coloplast.ceadmin://auth` |

### 1-2. 플랫폼 추가 — Android

| 항목 | 값 |
|---|---|
| Package name | `com.coloplast.ceadmin` |
| Signature hash (개발·사내 배포용) | `7oekJOas4sy8207R8KR+J7zwV/I=` |
| Signature hash (Play 스토어 배포용) | **확인 중** — Play 앱 서명 키의 SHA-1 로 계산해 회신드립니다 |

Redirect URI 는 위 두 값으로 각각 아래 형식이 됩니다.

```
msauth://com.coloplast.ceadmin/7oekJOas4sy8207R8KR%2BJ7zwV%2FI%3D
```

> **해시가 둘인 이유**: Play 에 AAB 로 올리면 구글이 앱을 자기 키로 다시 서명합니다(Play 앱 서명).
> 사내에서 직접 설치해 시험하는 판과 스토어에서 내려받는 판의 서명이 서로 달라, 둘 다 등록해야
> 양쪽에서 로그인이 됩니다.

### 1-3. 퍼블릭 클라이언트 흐름 허용

Authentication → **Allow public client flows: 예**

모바일 앱은 Client Secret 을 안전하게 보관할 수 없어, 시크릿 없이 PKCE 로 로그인합니다.

---

## 2. 확인 부탁드릴 것

1. **조건부 액세스(Conditional Access) 정책**이 이 앱 등록에 걸려 있습니까?
   - 기기 등록(Intune)·앱 보호 정책·MFA 가 필요한지에 따라 앱이 쓸 라이브러리가 갈립니다.
     필요하면 MSAL(브로커 연동, Microsoft Authenticator 앱 사용), 아니면 표준 OIDC 로 붙입니다.
2. **로그인 허용 범위** — 이 앱 등록으로 로그인할 수 있는 사람이 콜로플라스트 코리아 테넌트
   구성원 전체입니까, 특정 그룹입니까?
3. **운영용 등록**을 따로 두실 계획입니까? 지금 값은 Dev/UAT 로 받았습니다. 운영 주소로
   서비스할 때 같은 등록을 쓸지, 새 등록을 받을지 알려 주십시오.

---

## 3. 등록 후 우리 쪽 작업

1. 앱: 로그인 라이브러리 적용(위 2-1 답변에 따라 결정) — 지금은 자리만 있고 「준비 중」으로 응답합니다.
2. 서버: 앱이 받은 Entra 토큰을 확인해 앱 토큰으로 바꾸는 API 경로 추가.
   웹은 세션 기반 경로(`/auth/entra/callback`)라 앱에서 쓸 수 없습니다.
3. 사용자 연결: Entra 계정(UPN)과 CE Admin 계정을 잇는 규칙 확정 — 이메일 일치 기준을 제안드립니다.

---

## 참고 — 웹 설정 현황 (이미 반영됨)

| 항목 | 값 |
|---|---|
| Redirect URI | `https://www.ceadmin.co.kr/auth/entra/callback` |
| 프런트채널 로그아웃 | `https://www.ceadmin.co.kr/auth/entra/frontchannel-logout` |
| 사용 여부 | 켜짐 |
| Client Secret | 서버 설정에 암호화 보관(화면에는 뒤 넉 자만 표시) |
