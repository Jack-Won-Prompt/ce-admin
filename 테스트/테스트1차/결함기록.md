# 테스트 1차 — 결함 기록 (2026-09-01)

| # | 화면 | 무엇이 잘못돼 있나 | 상태 |
|---|---|---|---|
| 1 | 주문 등록 | **거래처에 적은 송금자명이 화면에 오지 않는다.** 화면은 처방전 `caregiver_name`을 보고, 거래처 수정 창은 `patients.remitter_name`에 저장한다 — 두 곳이 다른 값을 본다 | 확인 |
| 2 | 주문 등록 | 거래처 수정 저장 뒤 **연락 상태·연락 선호 방식·Fax가 곧바로 화면에 반영되지 않는다**(새로고침하면 보인다) | 확인 |
| 3 | 주문 등록 · 관할 청구처 찾기 | **다른 지사를 다시 고르면 참고사항이 앞서 고른 지사의 주소로 남는다.** `boOuterUse()` 가 참고사항이 비어 있을 때만 채운다 — 잘못된 주소로 등록된다 | 확인 |
| 4 | 주문 등록 · 관할 청구처 찾기 | **관할 읍·면·동 칸이 숨겨져 있는데 저장은 그것을 반드시 요구한다.** 도로명 주소에는 읍면동이 없고 카카오 REST 키가 없어 자동으로도 안 채워져, 담당자가 적을 방법이 없다 → **관할 청구처를 등록할 수 없다** | 확인 · 막힘 |
| 5 | 주문 등록 · 관할 청구처 찾기 | 전화번호 **「1577-1000」이 「157-710-00」으로 잘못 끊긴다**(대표번호 꼴을 3-3-3 으로 읽는다) | 확인 |
| 6 | 주문 등록 · 위임동의 | **위임동의 SMS 가 「발송/발행 내역」에 남지 않는다.** `issueConsent()` 가 `smsService->send()` 를 곧바로 불러 `message_histories` 기록을 건너뛴다(활동 기록에만 남는다) | 확인 |
| 7 | 주문 등록 · 상담 | 「신규 상담」을 누르면 **처방전 줄이 하나 더 생기고 주문도 함께 만들어진다**(RX-20260901-004 · ORD-0032). 같은 건에 상담만 더하려던 것이 빈 주문을 남긴다 | 확인 |
| 8 | 위드웍스 연동 | **판매주문의 단가가 우리 값이 아니다.** 우리 소비자가 2,250원인데 저쪽 판매주문에는 **1,804원**(판매금액 974,160)으로 섰다 — 계약은 「단가는 우리 것」 | 확인 · 저쪽과 함께 볼 것 |
| 9 | 위드웍스 연동 | 우리는 `confirm=false` 로 보내는데 저쪽 화면의 **확정 수량이 540 으로 차 있다**. 「확정은 창고에서」라는 약속과 다르다 | 확인 · 저쪽과 함께 볼 것 |
| 10 | 주문 등록 · 병원 조회 | **쌓아 둔 병원을 고를 수 없다.** 줄의 `onclick` 안에 `JSON.stringify(병원명)` 을 넣어, 그 큰따옴표가 속성의 큰따옴표와 부딪혀 속성이 끊긴다 — 누르면 「Unexpected end of input」. 「새 병원 등록」으로만 채울 수 있어 있는 병원도 매번 새로 만들게 된다 | **고침** |
| 11 | 주문 등록 · 관할 청구처 | **광역시의 한 글자 구(중구ㆍ동구ㆍ서구ㆍ남구ㆍ북구)를 못 읽어 관할을 찾지 못한다.** 시군구 규칙이 두 글자 이상만 읽고 있었다 — 「대구 중구 …」는 영영 못 찾는다 | **고침** |
