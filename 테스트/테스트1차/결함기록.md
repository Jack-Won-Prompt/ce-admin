# 테스트 1차 — 결함 기록 (2026-09-01)

| # | 화면 | 무엇이 잘못돼 있나 | 상태 |
|---|---|---|---|
| 1 | 주문 등록 | **거래처에 적은 송금자명이 화면에 오지 않는다.** 화면은 처방전 `caregiver_name`을 보고, 거래처 수정 창은 `patients.remitter_name`에 저장한다 — 두 곳이 다른 값을 본다 | 확인 |
| 2 | 주문 등록 | 거래처 수정 저장 뒤 **연락 상태·연락 선호 방식·Fax가 곧바로 화면에 반영되지 않는다**(새로고침하면 보인다) | 확인 |
| 3 | 주문 등록 · 관할 청구처 찾기 | **다른 지사를 다시 고르면 참고사항이 앞서 고른 지사의 주소로 남는다.** `boOuterUse()` 가 참고사항이 비어 있을 때만 채운다 — 잘못된 주소로 등록된다 | 확인 |
| 4 | 주문 등록 · 관할 청구처 찾기 | **관할 읍·면·동 칸이 숨겨져 있는데 저장은 그것을 반드시 요구한다.** 도로명 주소에는 읍면동이 없고 카카오 REST 키가 없어 자동으로도 안 채워져, 담당자가 적을 방법이 없다 → **관할 청구처를 등록할 수 없다** | 확인 · 막힘 |
| 5 | 주문 등록 · 관할 청구처 찾기 | 전화번호 **「1577-1000」이 「157-710-00」으로 잘못 끊긴다**(대표번호 꼴을 3-3-3 으로 읽는다) | 확인 |
| 6 | 주문 등록 · 위임동의 | **위임동의 SMS 가 「발송/발행 내역」에 남지 않는다.** `issueConsent()` 가 `smsService->send()` 를 곧바로 불러 `message_histories` 기록을 건너뛴다(활동 기록에만 남는다) | 확인 |
| 7 | 주문 등록 · 상담 | 「신규 상담」을 누르면 **처방전 줄이 하나 더 생기고 주문도 함께 만들어진다**(RX-20260901-004 · ORD-0032). 같은 건에 상담만 더하려던 것이 빈 주문을 남긴다 | 확인 |
| 8 | 위드웍스 연동 | **판매주문의 단가가 우리 값이 아니다.** 우리 소비자가 2,250원인데 저쪽 판매주문에는 **1,804원**(판매금액 974,160)으로 섰다 — 계약은 「단가는 우리 것」 | 확인 · 저쪽과 함께 볼 것 |
| 9 | 위드웍스 연동 | 우리는 `confirm=false` 로 보내는데 저쪽 화면의 **확정 수량이 540 으로 차 있다**. 「확정은 창고에서」라는 약속과 다르다 | 확인 · 저쪽과 함께 볼 것 |
| 10 | 주문 등록 · 병원 조회 | **쌓아 둔 병원을 고를 수 없다.** 줄의 `onclick` 안에 `JSON.stringify(병원명)` 을 넣어, 그 큰따옴표가 속성의 큰따옴표와 부딪혀 속성이 끊긴다 — 누르면 「Unexpected end of input」. 「새 병원 등록」으로만 채울 수 있어 있는 병원도 매번 새로 만들게 된다 | **고침** |
| 11 | 주문 등록 · 관할 청구처 | **광역시의 한 글자 구(중구ㆍ동구ㆍ서구ㆍ남구ㆍ북구)를 못 읽어 관할을 찾지 못한다.** 시군구 규칙이 두 글자 이상만 읽고 있었다 — 「대구 중구 …」는 영영 못 찾는다 | **고침** |
| 12 | 설정 | **카카오 로컬 REST 키가 없어 지자체 관할(시군구청ㆍ주민센터)을 밖에서 찾지 못한다.** 지자체 청구 건은 후보가 하나도 뜨지 않고 공단 지사만 나온다. 도로명 주소에서 행정동을 못 짚는 것도 같은 까닭 — 환경 설정 ▸ 카카오 ▸ 로컬 REST 키 | **설정 필요** |
| 13 | 주문 등록 · 주문 생성 | **막히는 까닭이 화면에 남지 않고 단추가 「생성 실패」에 붙박인다.** 서버는 422로 「재구매 가능일 2주 이상 남아 진행불가(2026-11-22 · 82일 남음)」를 돌려주는데, 알림이 곧 사라지고 단추가 되돌아오지 않아 다시 누를 수 없다 | **고침** |
| 14 | 주문 등록 · 주문 생성 | **청구하지 않는 건(처방외ㆍ산재ㆍ자동차보험)이 주문을 만들 수 없다.** 화면은 위임동의 단추를 감추는데(청구처가 「해당 없음」이라) 주문 생성은 「요양비 위임 동의가 아직입니다」로 막는다 — 감춰진 것을 받아 오라며 세운다 | **고침** |
| 15 | 주문 등록 | **유형을 「처방외」로 골라도 청구처가 「해당 없음」으로 서지 않는다.** 청구처를 자격(일반ㆍ기초…)만 보고 정하고 유형은 보지 않는다 — 처방외는 자격을 고를 것도 없어 청구처가 빈 채로 남고, 주문 관리에서 청구 단추가 어느 갈래인지 정하지 못한다 | **고침** |
| 16 | 청구 자료 묶음 | **공단ㆍ지자체에 낼 개인정보 동의서ㆍ위임동의서가 늘 빠진다.** 첨부는 `public` 디스크에 쌓이지만 동의서는 기본 디스크(`private`)에 들어가는데, 묶음이 `public` 만 보고 있었다 — 반려된 뒤에야 드러날 결함 | **고침** |
| 17 | 청구 자료 묶음 | **요양비 지급청구서가 줄만 있고 파일이 없어도 다시 만들지 않는다.** DB 줄만 보고 넘겨, 파일이 지워지거나 다른 서버에서 만든 줄이 남으면 묶음에서 영영 빠진다 | **고침** |
| 18 | 정산/회계 | 「주문금액」 칸이 **총액이 아니라 단가**를 보여 준다 — 121만 원짜리 주문이 2,250원으로 보인다. 칸 이름을 「단가」로 바로잡음 | **고침** |
| 19 | 주문 등록 · 증빙 발행 | **손으로 여는 발행 창이 청구전략을 보지 않는다.** 일반 건(세금계산서 90% · 기관 1,093,500)인데 창이 환자가 낼 돈(121,500)으로 열려, 자동 발행과 다른 금액이 나간다 | **고침** |
| 20 | 현금영수증 화면 | 오늘 **발행하고 취소한 3건이 조회되지 않는다.** 기간은 오늘로 맞아 있고 「상태ㆍ취소 사유」 칸도 있는데 「검색 결과가 없습니다」 — 취소분이 목록에서 통째로 빠지는지 확인 필요 | 확인 |
| 21 | 청구 관리 · 공단 | **「청구 완료로 표시」가 메모 없이 누르면 500 으로 죽는다.** `$data['memo']` 를 그냥 읽는데 validate 가 nullable 이라 키 자체가 없다 — 메모는 선택 항목이라 대개 비운다 | **고침** |
| 22 | 청구 관리 · 지자체 | 등기 발송 기록도 같은 꼴 — **등기번호 없이 저장하면 500** (`$data['registered_no']`) | **고침** |
